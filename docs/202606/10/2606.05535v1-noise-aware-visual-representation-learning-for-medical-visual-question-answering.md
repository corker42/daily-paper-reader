---
title: Noise-Aware Visual Representation Learning for Medical Visual Question Answering
title_zh: 噪声感知的医学视觉问答视觉表示学习
authors: "I Putu Adi Pratama, Bahadorreza Ofoghi, Atul Sajjanhar, Shang Gao"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.05535v1"
tags: ["query:retina-agent"]
score: 8.0
evidence: 利用去噪自编码器和LLM的噪声感知医学视觉问答
tldr: 医学视觉问答（Med-VQA）中现有方法常忽略视觉表示中的噪声问题。本文提出噪声感知框架，在视觉嵌入映射到LLM前引入去噪自编码器，通过重构干净嵌入学习鲁棒表示，再经MLP投影为视觉前缀token，并采用LoRA高效微调。在SLAKE和PathVQA上实验表明，该方法提升了噪声鲁棒性，同时保持竞争性干净性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05535v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1199, \"height\": 837, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05535v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1244, \"height\": 474, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.05535v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 699, \"height\": 193, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05535v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1257, \"height\": 436, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05535v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1089, \"height\": 307, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05535v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1096, \"height\": 309, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05535v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1086, \"height\": 309, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05535v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1098, \"height\": 311, \"label\": \"Table\"}]"
motivation: 现有Med-VQA方法忽略视觉表示中的噪声和小变化，导致鲁棒性不足，影响临床决策支持的有效性。
method: 在视觉嵌入进入LLM前，使用去噪自编码器重建干净嵌入，再经MLP投影为视觉前缀，通过LoRA参数高效微调整个框架。
result: 在SLAKE和PathVQA上，该方法在噪声输入下性能显著提升，同时保持干净输入下的竞争性结果。
conclusion: 学习鲁棒的视觉表示可有效增强Med-VQA的性能和鲁棒性，为临床决策支持提供更可靠的技术方案。
---

## 摘要
医学视觉问答（Med-VQA）通过使AI模型能够解释医学图像并回答临床相关问题，在临床决策支持方面具有巨大潜力。最近的方法通常通过轻量级映射网络将现成的视觉编码器与大语言模型（LLM）连接起来，以降低计算成本。然而，这些方法往往忽视了处理视觉表示中噪声和微小无关变化的重要性。为解决这些问题，我们提出了一种噪声感知的Med-VQA框架，该框架在视觉嵌入映射到LLM输入空间之前，引入了一个去噪自编码器。去噪自编码器经过预训练，能够从受损输入中重建干净的视觉嵌入，从而鼓励模型学习对噪声不敏感的鲁棒视觉表示。得到的嵌入随后通过多层感知机（MLP）投影到语言模型嵌入空间中，形成为LLM提供图像信息的视觉前缀令牌。为了实现无需完全重新训练的高效适配，我们采用低秩适配（LoRA）进行参数高效微调。所提出的方法在SLAKE和PathVQA基准上进行了评估。实验结果表明，该方法对噪声输入嵌入具有更强的鲁棒性，同时在多个评估标准上保持了具有竞争力的干净性能。这些发现表明，学习更鲁棒的视觉表示可以提升Med-VQA的性能和鲁棒性。

## Abstract
Medical visual question answering (Med-VQA) has strong potential for clinical decision support by enabling AI models to interpret medical images and answer clinically relevant queries. Recent approaches typically connect off-the-shelf vision encoders with large language models (LLMs) through lightweight mapping networks to reduce computational cost. However, these methods often overlook the importance of handling noise and small irrelevant changes in visual representations. To address these challenges, we propose a noise-aware Med-VQA framework that incorporates a denoising autoencoder before visual embeddings are mapped into the input space of an LLM. The denoising autoencoder is pretrained to reconstruct clean visual embeddings from corrupted inputs, encouraging the model to learn robust visual representations that are less sensitive to noise. The resulting embeddings are then projected into the language model embedding space using a multi-layer perceptron (MLP), forming visual prefix tokens that provide image information to the LLM. To enable efficient adaptation without full retraining, we employ parameter-efficient fine-tuning using low-rank adaptation (LoRA). The proposed method is evaluated on the SLAKE and PathVQA benchmarks. Experimental results show improved robustness to noisy input embeddings while maintaining competitive clean performance across multiple evaluation criteria. These findings suggest that learning more robust visual representations can enhance Med-VQA performance and robustness.

---

## 论文详细总结（自动生成）

# 噪声感知的医学视觉问答视觉表示学习 — 论文总结

## 1. 论文的核心问题与整体含义
- **研究动机**：医学视觉问答（Med-VQA）旨在让AI理解医学图像并回答临床问题，具有辅助诊断的潜力。现有方法通常使用冻结的视觉编码器和大语言模型（LLM），通过轻量级映射网络连接以减少计算开销。但这些方法忽略了一个关键问题：视觉编码器生成的嵌入表示中可能包含噪声和微小无关变化（例如医学图像采集传输过程中引入的噪声），这些噪声会在映射过程中传入LLM，影响答案生成的稳定性和准确性。
- **整体目标**：提出一种**噪声感知的视觉表示学习框架**，在视觉嵌入映射到LLM之前，通过去噪自编码器（Denoising Autoencoder, DAE）学习鲁棒的表示，从而提升Med-VQA在干净和噪声条件下的性能与鲁棒性。

## 2. 提出的方法论
- **核心思想**：两阶段训练策略，将去噪表示学习与生成对齐解耦。
  - **阶段1（去噪自编码器预训练）**：用冻结的CLIP视觉编码器提取图像全局嵌入 \( v \in \mathbb{R}^{512} \)。对该嵌入添加高斯噪声（\( \tilde{v} = v + \epsilon, \epsilon \sim \mathcal{N}(0, \sigma^2 I) \)），通过一个瓶颈MLP编码器将 \( \tilde{v} \) 压缩为128维潜在表示 \( z \)，再经解码器重建干净嵌入 \( \hat{v} \)。使用Smooth L1损失优化重建。
  - **阶段2（Med-VQA训练）**：丢弃解码器，保留冻结的DAE编码器，用其生成鲁棒的潜在表示 \( z \)。然后将 \( z \) 通过一个3层MLP映射器投影为LLM嵌入空间中的视觉前缀令牌（8个token，维度1600）。这些前缀令牌与问题文本嵌入拼接，输入冻结的GPT-2 XL，通过自回归方式生成答案。根据实验设置，可以完全冻结LLM或使用LoRA进行参数高效微调。

- **关键技术细节**：
  - 噪声类型：高斯噪声（连续向量，可控、可复现）。
  - DAE架构：512→256→128（瓶颈）→256→512，ReLU激活，dropout 0.1。
  - 视觉前缀长度8，GPT-2 XL嵌入维度1600。
  - 使用LoRA微调LLM的注意力参数，避免灾难性遗忘。

- **公式**：重建损失 \( \mathcal{L}_{DAE} = \frac{1}{N} \sum_i \left( \frac{1}{2}(\hat{v}_i - v_i)^2 \text{ if } |\hat{v}_i - v_i|<1 \text{ else } |\hat{v}_i - v_i| - \frac{1}{2} \right) \)；VQA损失为交叉熵 \( \mathcal{L}_{VQA} = -\sum_{t \in A} \log p_\theta(y_t | y_{<t}, q, I) \)。

## 3. 实验设计
- **数据集**：
  - **SLAKE**（放射学图像，14,028个问题，461个唯一答案）
  - **PathVQA**（组织病理学图像，32,799个问题，3,182个唯一答案）
- **评估指标**：BLEU-1、BERTScore、F1、Accuracy（总体及按问题类型）。
- **对比方法**：
  - 基线（Baseline）：直接使用CLIP嵌入经MLP映射到LLM。
  - 自编码器变体（AE）：使用标准自编码器（无噪声注入）预处理嵌入。
  - 提出的去噪自编码器（DAE）：使用噪声注入训练。
- **实验场景**：
  - 冻结LLM vs. LoRA微调LLM。
  - 干净评估（无推理噪声）。
  - 鲁棒性评估：在推理时向视觉嵌入注入高斯噪声（σ=0.1,0.5,1.0），并评估DAE在不同阶段1噪声水平（σ=0.1,0.5,1.0）下的表现。

## 4. 资源与算力
- 论文未明确说明使用的GPU型号、数量或训练时长。仅提及使用**冻结的CLIP ViT-B/32视觉编码器**和**GPT-2 XL语言模型**，视觉嵌入预计算，训练采用两阶段方式。由于未提供具体硬件信息，无法评估算力成本。

## 5. 实验数量与充分性
- **实验数量**：约10张表格（表2~6），包含：
  - 干净性能对比（表2，2个数据集×2个微调设置×3个方法）。
  - 不同阶段1噪声水平的鲁棒性评估（表3和表5，每个数据集×2个设置×3个噪声水平）。
  - 鲁棒性综合对比（表4和表6，每个数据集×2个设置×3个方法）。
- **充分性**：
  - 消融实验设计合理（Baseline vs AE vs DAE），区分了无噪声注入和噪声注入的影响。
  - 鲁棒性评估系统化，覆盖不同噪声强度和解冻策略。
  - 但仅使用了两个数据集（一个放射、一个病理），未在更多模态（如超声、MRI）上验证；且只使用了一个视觉编码器（CLIP）和一个语言模型（GPT-2 XL），泛化性有限。

## 6. 主要结论与发现
- 提出的DAE框架在干净评估下，SLAKE上表现最佳（LoRA下BLEU-1=0.796，Acc=0.756）；PathVQA上AE略优但DAE在噪声下更鲁棒。
- 在噪声条件下，DAE显著优于基线和AE。例如SLAKE上LoRA的**Noisy Avg. Acc.**从0.642（基线）提升至0.735（DAE）；冻结设置下从0.473提升至0.713。PathVQA上也有类似的大幅提升。
- **中等阶段1噪声（σ=0.5）** 在干净性能和鲁棒性之间取得最佳平衡；过弱（σ=0.1）的噪声虽能提高干净准确率，但鲁棒性较差；过强（σ=1.0）则牺牲干净性能。
- 定性示例显示DAE能在基线出错时预测出正确答案。

## 7. 优点
- **创新性**：首次将去噪自编码器嵌入Med-VQA框架，直观且有效解决了视觉表示中噪声被传播到LLM的问题。
- **两阶段训练设计**：分离了鲁棒表示学习与生成对齐，使各阶段目标清晰，便于模块化改进。
- **系统化的鲁棒性评估**：通过控制噪声强度进行推理时扰动评估，提供了公平的对比基线。
- **参数高效**：使用LoRA微调LLM，避免全参数重训，适配医疗领域。
- **结果稳定**：在两个不同模态数据集上均验证了鲁棒性提升。

## 8. 不足与局限
- **噪声假设局限**：仅使用高斯噪声，无法覆盖真实医学图像中可能出现的结构化噪声、伪影或传感器噪声。未来需考虑更真实的噪声仿真。
- **映射网络简单**：MLP映射策略较为基础，未探索更高级的跨模态对齐机制（如Q-Former、交叉注意力），可能限制了表示融合能力。
- **模型泛化性**：仅采用单一CLIP视觉编码器和GPT-2 XL语言模型；未在更大规模的医疗多模态预训练模型（如BiomedCLIP、LLaVA-Med）上验证。
- **实验覆盖**：缺少对推理噪声水平更高或更复杂的扰动（如对抗性噪声）的评估；也未与最新Med-VQA方法（如MedBLIP、LLaVA-Med）做公平比较。
- **计算资源未报告**：缺乏GPU型号、训练时间等细节，不利于复现和效率评估。
- **干净性能并非总是最优**：在PathVQA上DAE的干净准确率略低于AE，说明去噪训练可能轻微损害干净条件下的性能，存在trade-off。

（完）
