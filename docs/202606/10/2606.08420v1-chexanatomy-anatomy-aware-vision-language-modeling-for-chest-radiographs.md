---
title: "CheXanatomy: Anatomy-Aware Vision-Language Modeling for Chest Radiographs"
title_zh: "CheXanatomy: 面向胸部X光片的解剖感知视觉语言建模"
authors: "Sergios Gatidis, Curtis Langlotz, Christian Bluethgen"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08420v1"
tags: ["query:retina-agent"]
score: 8.0
evidence: 医学诊断中的大语言模型
tldr: 现有视觉语言模型缺乏精细解剖结构编码，难以用于分割任务。本文提出CheXanatomy框架，通过自回归token空间监督将解剖知识注入预训练模型，以生成解剖掩码。利用CT合成胸片及前向投影标签实现大规模监督。在合成和真实数据上，该方法性能与U-Net相当，且域迁移鲁棒性更强，样本效率更高。结果表明嵌入解剖结构可促进空间理解。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08420v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1266, \"height\": 825, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08420v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1263, \"height\": 595, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08420v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1245, \"height\": 334, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08420v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1247, \"height\": 856, \"label\": \"Figure\"}]"
motivation: 视觉语言模型仅对齐全局语义，无法编码精细解剖结构，限制了在分割等空间任务中的应用。
method: 通过自回归下一个token预测训练模型生成解剖分割掩码，并利用CT合成胸片及前向投影标签实现可扩展监督。
result: 在合成与真实胸片上，该方法性能与U-Net相当，域迁移下几何鲁棒性更好，且样本效率更高。
conclusion: 将解剖结构嵌入生成目标可获得空间可解释表征，支持解剖感知的医学视觉语言建模。
---

## 摘要
在大规模图像-文本对上预训练的视觉语言模型（VLM）展现出强大的图像级理解能力，但其主要针对全局对齐进行优化，并未显式编码细粒度的解剖结构，从而限制了其在空间精确任务（如分割）中的适用性。我们提出CheXanatomy框架，该框架通过自回归令牌空间监督将显式解剖知识集成到预训练VLM中。该模型并非添加特定任务的解码头，而是通过下一令牌预测来训练生成解剖分割掩码。为了实现可扩展的监督，我们从CT容积中合成逼真的胸部X光片，并前向投影CT分割标签以获得解剖一致的2D掩码。我们在合成和真实胸部X光片上以U-Net为基准评估该方法，包括对模型规模、输入分辨率和视觉编码器微调的消融实验。自回归解剖监督在分布内实现了与专用卷积模型相当的性能，并在向真实CXR数据的域迁移下展现出更强的几何鲁棒性。此外，在有限监督下适应新型定位任务时，解剖预训练模型展现出更高的样本效率。更大的模型和更高的输入图像分辨率能提升性能，而视觉编码器微调的效果有限。这些结果表明，将解剖结构直接嵌入生成目标有助于获得空间化的表征，并支持解剖感知的医学视觉语言建模。

## Abstract
Vision-language models (VLMs) pretrained on large-scale image-text pairs demonstrate strong image-level understanding, but are primarily optimized for global alignment and do not explicitly encode fine-grained anatomical structure, limiting their suitability for spatially precise tasks such as segmentation. We introduce CheXanatomy, a framework that integrates explicit anatomical knowledge into a pretrained VLM through autoregressive token-space supervision. Instead of adding task-specific decoder heads, the model is trained to generate anatomical segmentation masks via next-token prediction. To enable scalable supervision, we synthesize realistic chest radiographs from CT volumes and forward-project CT segmentation labels to obtain anatomically consistent 2D masks. We evaluate the approach on synthetic and real chest radiographs against a U-Net baseline, including ablations on model scale, input resolution, and vision encoder fine-tuning. Autoregressive anatomical supervision achieves performance comparable to specialized convolutional models in-distribution and demonstrates improved geometric robustness under domain shift to real CXR data. In addition, anatomy-pretrained models exhibit improved sample efficiency when adapting to novel localization tasks under limited supervision. Larger models and higher input image resolution improve performance, while vision encoder fine-tuning has limited effect. These results show that embedding anatomical structure directly into the generative objective promotes spatially grounded representations and supports anatomy-aware medical vision-language modeling.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：现有的视觉语言模型（VLM）在大规模图像-文本对上预训练后，擅长图像级的全局语义对齐，但缺乏对细粒度解剖结构的显式编码。这限制了它们在需要空间精确度的任务（如医学图像分割）中的表现。
- **背景价值**：胸部X光片（CXR）的解剖结构分割因投影重叠、结构复杂和采集差异而困难。传统卷积模型（如U-Net）虽在分布内表现好，但依赖密集像素标注且易受域偏移影响。VLM虽有迁移能力，但其训练目标不包含空间结构化知识。为此，本文提出在VLM中显式注入解剖知识，以实现空间可解释的表征。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：将解剖分割任务转化为自回归的下一令牌预测问题，利用VLM自身的生成式目标进行训练，无需添加专用的解码头。同时，通过CT容积到合成X光片的投影生成大规模、一致的解剖标注，弥补真实CXR标注稀缺的问题。
- **关键技术细节**：
  - **模型架构**：基于PaliGemma 2（SigLIP-So400m视觉编码器 + Gemma 2语言模型）。分割掩码被编码为16个VQ-VAE离散化令牌，预测这些令牌即可重建分割掩码。训练使用标准的下一个令牌预测交叉熵损失，无像素级损失。
  - **合成数据生成**：从CT-RATE数据集的非增强CT容积中，通过可微DRR（Diff-DRR）生成前后位和侧位投影X光片（共25,968训练图，1,366测试图）。使用TotalSegmentator获得3D解剖分割，再前向投影到2D获得与合成X光片对齐的80个解剖结构掩码（涵盖胸部骨骼、肺、心脏、大血管、上腹器官等）。通过随机缩放模拟解剖大小和采集几何的变异。
  - **训练策略**：采用LoRA参数高效微调，保持预训练权重固定（或微调视觉编码器）。实验了3B和10B参数版本，输入分辨率224×224和448×448。
  - **提示设计**：针对每个解剖标签采样多个同义词构造提示。

## 3. 实验设计

- **数据集**：
  - **合成数据**：CT-RATE留出的1,366张测试合成X光片（前后位+侧位）。
  - **真实数据**：CheXmask（200张前后位X光片，含心/肺掩码）、VinDr-RibCXR（198张前后位X光片，含双侧肋骨分割）。
  - **少样本迁移任务**：RadVLM视觉定位数据集（118,360训练样本，22,972测试样本），包含多种解剖结构定位。
- **基准模型**：二维U-Net（4阶段编码器+解码器，80个输出通道），使用相同合成训练数据，输入分辨率448×448。
- **评价指标**：Dice系数、IoU、Hausdorff距离、傅里叶描述子距离（FDD）用于分割；边界框IoU用于定位。
- **消融实验**：模型规模（3B vs 10B）、输入分辨率（224 vs 448）、视觉编码器是否微调。

## 4. 资源与算力

- 文中明确提到：训练使用8块NVIDIA H100 GPU。不同配置下batch size为8（10B/448模型）到24（3B/224模型）。未说明训练总时长，但提及使用LoRA进行参数高效微调，整体算力需求中等。

## 5. 实验数量与充分性

- 实验较为充分：
  - 在合成数据上比较了多种VLM配置与U-Net的全面指标（4个指标）。
  - 在两个真实数据集（CheXmask、VinDr-RibCXR）上评估域迁移性能。
  - 进行了3项消融：模型规模、分辨率、视觉编码器微调。
  - 在RadVLM上做了少样本迁移实验（0%–100%数据比例），比较了有无解剖预训练的效果。
  - 提供了图例展示典型分割结果（图3）和定位示例（图4）。
- **公平性**：U-Net使用相同合成数据训练，VLM配置比较标准，指标选择全面（重叠+边界+形状），消融覆盖关键因素。但未与其他医学VLM（如CheXzero、GLoRIA）或基于SAM的方法对比，降低了比较的完整性。

## 6. 主要结论与发现

- **分布内性能**：10B/448的VLM在合成数据上Dice和IoU接近U-Net，且Hausdorff距离和FDD更低，边界和形状更一致。
- **域迁移鲁棒性**：在真实CXR上，VLM的Dice/IoU与U-Net相当，但边界误差（Hausdorff）和形状误差（FDD）显著降低，几何稳定性更强。
- **模型与输入的影响**：增大模型规模和输入分辨率可提升性能；微调视觉编码器无明显影响，表明解剖推理主要在令牌空间发生。
- **样本效率**：解剖预训练模型在少样本（0-3%数据）下明显优于无预训练模型，随机特定数据增加差距收敛，说明提供了更好的空间先验。
- **整体贡献**：证明了在生成式目标中嵌入解剖结构可促进空间化表征，为医学VLM提供可扩展、轻量的解剖感知能力。

## 7. 优点

- **方法创新**：首次将显式解剖知识通过令牌空间自回归目标注入预训练VLM，无需修改架构或添加解码头，保持模型统一性。
- **数据可扩展性**：利用CT容积投影大规模生成合成X光片与精细解剖标注，缓解真实标注匮乏问题，且标签几何一致。
- **参数高效**：使用LoRA微调，仅更新少量参数，训练成本低。
- **鲁棒性提升**：在域迁移下表现出更稳定的几何结构，优于传统卷积模型。
- **通用性**：支持语言提示驱动，可灵活扩展新解剖结构，适用于报告生成、区域推理等下游任务。

## 8. 不足与局限

- **依赖合成数据质量**：合成X光片与真实CXR在纹理、噪声、解剖形态上仍有差距，可能影响泛化上限。虽然实验显示域迁移改善，但未评估对多种临床中心数据的泛化。
- **对比不充分**：未与其他医学VLM（如CheXzero、CONCH）或SAM系列（如MedSAM）比较，无法判断是否达到该领域最优。
- **任务覆盖狭窄**：仅实验了分割和定位任务，未在报告生成、分类等场景验证解剖预训练的效用。
- **偏差风险**：CT-RATE数据集可能无法完全代表真实CXR的病理分布；TotalSegmentator的3D分割错误会被传播到2D标签。
- **计算资源信息不完整**：未提供训练总时长，使得复现成本不明确。
- **提示工程未充分讨论**：多同义词采样策略对性能的影响未做消融。

（完）
