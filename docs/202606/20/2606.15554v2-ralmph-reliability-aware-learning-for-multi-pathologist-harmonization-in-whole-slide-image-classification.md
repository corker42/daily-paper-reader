---
title: "RaLMPH: Reliability-aware Learning for Multi-Pathologist Harmonization in Whole-Slide Image Classification"
title_zh: RaLMPH：面向全切片图像分类的多病理学家协调的可靠性感知学习
authors: "Sungrae Hong, Jiwon Jeong, Soeun Cheon, Donghee Han, Sol Lee, Jisu Shin, Kyungeun Kim, Mun Yong Yi"
date: 2026-06-14
pdf: "https://arxiv.org/pdf/2606.15554v2"
tags: ["query:fundus-class"]
score: 8.0
evidence: 用于医学图像分类的多标注者标签协调
tldr: 病理切片分析中多位病理学家标注存在分歧，现有方法难以适应多实例学习。提出RaLMPH框架，利用可靠性场联合建模特征空间邻域结构与专家不确定性，实现样本级局部标注者排序与自适应门控标签融合。在临床数据集和模拟基准上，RaLMPH持续超越现有方法，有效提升标签协调与下游分类性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15554v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1261, \"height\": 442, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15554v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1264, \"height\": 385, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15554v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1263, \"height\": 239, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15554v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1266, \"height\": 175, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15554v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1263, \"height\": 175, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.15554v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1259, \"height\": 1014, \"label\": \"Table\"}]"
motivation: 现有MIL方法假设单金标准标签，忽视病理学家间标注分歧，而现有多标注者学习方法不适用于MIL和局部诊断场景。
method: RaLMPH引入可靠性场建模特征空间局部邻域与专家熵，进行样本级可信邻域识别与标注者排序，并通过自适应门控融合标签。
result: 在六位病理学家标注的临床WSI数据集及模拟基准上，RaLMPH持续优于现有基线方法，提升了标签协调与下游MIL性能。
conclusion: RaLMPH通过可靠性感知机制有效融合多位病理学家标注，为多标注者MIL提供了有效的标签协调方案。
---

## 摘要
多实例学习（MIL）是全切片图像（WSI）分析的标准范式，并在计算病理学中取得了显著成果。然而，大多数MIL流程假设每张切片只有一个“金标准”标签，这与临床实践中常见的病理学家间显著变异性相冲突。现有的多标注者学习和标签细化方法通常估计全局标注者可靠性或依赖于单实例假设，使其不适合MIL以及专家意见不一致的局部诊断场景。我们提出了RaLMPH（面向多病理学家协调的可靠性感知学习），一种基于MIL的标签协调框架，用于由多位病理学家标注的WSI。RaLMPH引入了一个可靠性场，联合建模（i）WSI特征空间中的局部邻域结构和（ii）专家不确定性（熵），从而能够逐样本识别可信的参考邻域。利用该场，RaLMPH执行逐样本局部标注者排序，以选择每张切片的可靠意见，并应用自适应门控机制基于局部可靠性融合标签。在来自六位病理学家的临床WSI数据集以及受控模拟基准上的实验表明，RaLMPH始终优于现有方法。进一步的分析阐明了我们的可靠性感知机制如何改进标签协调和下游MIL性能。

## Abstract
Multiple Instance Learning (MIL) is a standard paradigm for Whole-Slide Image (WSI) analysis and has achieved strong results in computational pathology. However, most MIL pipelines assume a single "gold" label per slide, which conflicts with clinical practice where substantial inter-pathologist variability is common. Existing multi-annotator learning and label-refinement methods typically estimate global annotator reliability or rely on single-instance assumptions, making them poorly suited to MIL and to localized diagnostic contexts where experts disagree. We propose RaLMPH (Reliability-aware Learning for Multi-Pathologist Harmonization), a MIL-based label reconciliation framework for WSIs annotated by multiple pathologists. RaLMPH introduces a reliability field that jointly models (i) local neighborhood structure in WSI feature space and (ii) expert uncertainty (entropy), enabling per-sample identification of trustworthy reference neighborhoods. Leveraging this field, RaLMPH performs sample-wise local annotator ranking to select reliable opinions per slide and applies an adaptive gating mechanism to fuse labels conditioned on local reliability. Experiments on a clinical WSI dataset with labels from six pathologists, as well as controlled simulated benchmarks, show that RaLMPH consistently outperforms existing approaches. Further analyses clarify how our reliability-aware mechanism improves label reconciliation and downstream MIL performance.

---

## 论文详细总结（自动生成）

# RaLMPH 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：在计算病理学中，全切片图像（WSI）分类通常采用多实例学习（MIL）范式。但现有MIL方法普遍假设每张WSI只有一个“金标准”标签，这与临床实际严重不符——多位病理学家对同一张切片的诊断常存在显著分歧（即“标注者间变异性”）。这种分歧并非单纯的噪声，而是疾病复杂性和专家专业差异的体现。
- **研究动机**：直接将多个病理学家的标签通过简单统计（如多数投票、软标签）或传统多标注者学习方法处理时，存在两个关键缺陷：
    1.  传统方法往往假设标注者具有全局可靠性（即某位专家整体上更可靠），但实际上，不同病理学家在不同局部分析点上可能各有专长（局部权威性）。
    2.  多数多标注者方法面向单实例数据点，无法直接应用于由数千个实例（图像块）构成的WSI实例袋（bag）场景。
- **整体含义**：本文旨在提出一种专为MIL设计的标签协调框架，能够在保留多位专家局部诊断权威的同时，为WSI生成更可靠的聚合标签，从而提升下游MIL分类性能。

## 2. 方法论：核心思想、关键技术细节

### 核心思想
- **可靠性场（Reliability Field）**：联合建模WSI特征空间中的局部邻域结构和专家不确定性的度量，用于识别每个样本的可信参考邻域。
- **逐样本局部专家排序（Sample-wise Local Expert Sorting）**：不为所有样本固定专家排名，而是根据当前样本的邻域信息动态排序，选择最可靠的专家意见。
- **自适应门控标签融合（Gated Label Reconciling）**：根据局部标签一致性程度，自适应地决定采用多数软标签还是通过邻域加权融合的标签。

### 技术细节（算法流程）
1. **数据预处理**：
   - 使用病理学基础模型（如CONCH）提取每个WSI的实例特征 `z_n`（特征袋）。
   - 多位专家（J位）为每张WSI提供软标签 `s_n`（C个类别的概率分布）。

2. **可靠性场构建**（公式1）：
   - 定义距离度量 `d_Φ(z_n, z_m)`，其中权重矩阵 `M_nm` 同时编码了特征袋的方差 `σ²_n`（通过特征dropout多次计算）和专家标签的熵 `H(¯s_n)`。
   - 该距离将“样本模糊性”和“专家非共识性”较大的样本在空间中推开，从而形成邻域时自动偏向可靠样本。

3. **门控函数**（公式2）：
   - 计算 `G_n`，用于控制最终标签是更接近原始软标签均值 `¯s_n` 还是邻域融合标签。
   - 当样本标签熵低（高共识）时，`G_n` 接近1，保留原始均值；当熵高时，转向邻域信息，但若邻域也缺乏共识，则保留一定比例。

4. **局部专家排序**（公式3）：
   - 对每个样本，在其邻域内计算每位专家标签与该邻域软标签均值的一致性（余弦相似度），得到专家排名列表 `J_n^K`。

5. **标签融合**（公式4）：
   - 从K个邻居中自适应选择 `K*` 个邻居（避免过度平滑，公式5）。
   - 融合公式：`˜y_n = G_n × ¯s_n + (1 - G_n) × Σ_k W_k · ˙s_kn`
   - 其中 `W_k` 是基于可靠性场距离的权重，`˙s_kn` 是根据专家排名聚合后的单专家意见。

6. **训练**：使用聚合后的软标签 `˜y_n` 训练任意MIL模型（如TransMIL、DTFD-MIL-AFS），损失函数为交叉熵。

## 3. 实验设计

### 数据集
| 数据集 | 类型 | 规模 | 标注情况 |
|--------|------|------|----------|
| BRACS (公共) | 乳腺癌亚型 | 547张WSI，7类 | 无真实多标注，使用模拟 |
| BCNB (公共) | 乳腺癌分子亚型 | 1058张活检WSI，4类 | 无真实多标注，使用模拟 |
| In-house (私有) | 大肠息肉分类 | 698张WSI训练集（6位专家，共4188条标注）+ 684张测试集（有共识标签） | 真实多位病理学家标注 |

### 对比方法（Benchmark）
- **基线**：多数投票（MV）、软标签（SL）、集成（Ensemble）
- **传统多标注者方法**：GLAD（基于隐藏状态）、DL-CL（多分支架构）、NWVNC（数据校正）
- **基于特征参考的最近方法**：IWBVT（实例加权）、KFNN（K-自由近邻）

### 实验设置
- **MIL骨干**：TransMIL、DTFD-MIL-AFS
- **模拟专家生成**：对公共数据集，通过聚类和Dirichlet分布为各区域分配不同可靠性的合成专家。
- **超参数**：K=10，τ=0.1；采用固定种子确保可重复性。

## 4. 资源与算力

- **GPU**：NVIDIA® RTX A6000（单卡）
- **训练时长**：文中未明确给出训练一个模型的绝对时长，但在时间效率分析（图4）中比较了各方法的预处理/训练时间（单位：分钟）。例如：MV/SL较短，RaLMPH预处理约16-18分钟，但优于Ensemble等的线性耗时。
- **种子**：所有实验使用固定种子。

## 5. 实验数量与充分性

- **主要实验**：在3个数据集（2个公共模拟+1个真实临床）上，使用2种MIL骨干，共进行了6组主实验（表1），每组报告Accuracy和AUC。
- **消融实验**：对RaLMPH的3个核心组件（Φ、Jkn、自适应K*）分别进行了消融，验证各组件有效性。
- **鲁棒性分析**：模拟减少专家数量（J'从2到5），在In-house数据集上评估所有方法表现（图3）。
- **敏感性分析**：对K（5,10,15）和τ（0.05-1.0）进行了网格搜索（图5），结果稳定。
- **定性分析**：使用t-SNE可视化L2距离与Φ距离下的样本分布，佐证方法效果（图2）。
- **时间效率**：给出了各方法的预处理+训练总耗时对比（图4）。
- **充分性评价**：实验设计较为全面，覆盖了多种场景（公共/私有、真实/模拟、不同专家数），对比方法多样，消融和敏感性分析完整。但缺少在实际临床场景中跨中心验证（仅一个In-house数据集），且未报告统计显著性检验（虽有p值标注，但仅用于部分比较）。

## 6. 主要结论与发现

- **核心结论**：RaLMPH在所有数据集和MIL骨干上一致优于现有方法，证明了可靠性场和局部专家排序在多标注者MIL中的有效性。
- **关键发现**：
  - 传统方法（如GLAD、DL-CL）假设全局可靠性，在真实临床场景中表现不佳。
  - 简单特征参考方法（如KFNN）因基于单实例假设，难以适应WSI的多实例特性。
  - 联合建模特征方差与标签熵的可靠性场能够更好地区分模糊样本与歧义样本。
- **实用性**：RaLMPH在减少专家数量时仍保持稳定，更符合现实可用标注者较少的情况。

## 7. 优点

1. **问题定义精准**：针对MIL场景中多专家标注分歧，且考虑局部权威性，填补了现有方法空白。
2. **模型设计新颖**：可靠性场巧妙融合了特征不确定性和标签不确定性；门控机制和局部专家排序都是针对WSI量身定制的。
3. **实验充分**：在真实临床数据集（6位病理学家）上验证，模拟实验也合理。
4. **消融透彻**：对各组件均进行消融，证明每个部分都有贡献。
5. **效率优异**：预处理后训练无额外开销，综合时间效率高于多数对比方法。
6. **泛化性**：在两个公共数据集和两个不同MIL骨干上表现一致，表明方法具有良好泛化能力。

## 8. 不足与局限

1. **依赖基础模型**：特征提取使用固定预训练模型，未探索端到端训练或微调。
2. **外推性限制**：当前方法假设所有标注者是专家（病理学家），不适用于众包或弱监督标注场景，作者在局限中明确指出了这一点。
3. **真实临床验证不足**：仅使用一家医疗中心的私有数据集，且测试集仅684张（虽有共识标签）。未来需在更多中心、更多疾病类型上验证。
4. **超参数选择**：K和τ虽经敏感性分析，但仍需手动预设，缺乏自适应版本。
5. **计算资源信息不完整**：未提供完整训练时间、参数量、内存占用等，难以复现效率对比。
6. **统计严谨性**：虽有p值标记（<0.05, <0.01），但仅对部分比较，未对多次实验进行多重检验校正等更严格的分析。

（完）
