---
title: "Mask to Concept: Auto-Promptable SAM3 via Efficient Test-Time Concept Embedding Search for Few-Shot Annotation"
title_zh: 从掩码到概念：通过高效测试时概念嵌入搜索实现自动提示的SAM3用于少样本标注
authors: "Quan Zhou, Shaoqing Zhai, Qiang Hu, Jia Chen, Qiang Li, Zhiwei Wang"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.26711v2"
tags: ["query:fundus-mc"]
score: 8.0
evidence: 医学图像少样本标注框架；可适配于眼科疾病分类。
tldr: 针对医学图像标注中现有方法依赖外部模块或人工文本提示的问题，提出Mask to Concept（M2C）框架。该方法仅需少量标注图像，在冻结的SAM3架构内自动搜索可迁移视觉概念嵌入，通过梯度更新最小化分割误差。结合混合不确定性估计（HUE）模块主动筛选高不确定样本请求人工修正，形成自增强标注循环。在医学分割基准上取得SOTA少量样本分割性能，显著提升标注效率，无需外部模块或参数重训练。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.26711v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1257, \"height\": 504, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26711v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1246, \"height\": 580, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26711v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1247, \"height\": 365, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26711v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 601, \"height\": 458, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.26711v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1272, \"height\": 402, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26711v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 613, \"height\": 279, \"label\": \"Table\"}]"
motivation: 现有自动化提示方法依赖外部特征匹配或辅助网络，引入架构开销且缺乏医学细粒度知识；手工文本提示模糊且不可扩展。
method: 提出M2C，利用可学习概念嵌入在冻结SAM3中搜索，通过梯度更新优化分割；HUE模块融合预测熵和框提示一致性，主动标记高不确定样本以闭环修正。
result: 在多个医学分割基准上达SOTA少量样本分割性能，标注效率显著优于现有方法，且无需额外网络或人工特征设计。
conclusion: M2C为医学图像自动标注提供高效、自增强的实用方案，克服了外部模块依赖和文本工程瓶颈。
---

## 摘要
将基础分割模型从人工提示工具转变为自动提示标注器对于可扩展的医学数据标注至关重要。当前方法通常依赖外部特征匹配器或辅助网络来自动化几何提示，但这引入了架构开销并限制了性能可扩展性。尽管SAM3通过可重用文本提示原生支持概念分割，但由于缺乏细粒度临床知识以及人工描述的不明确性，其在医学影像中的直接应用受到阻碍。在这项工作中，我们提出了Mask to Concept (M2C)，一个高效框架，无需外部模块、参数重新训练或手动文本工程，即可适配SAM3用于医学少样本标注。仅使用少量标注图像，M2C使SAM3能够在其冻结架构内自动搜索可转移的视觉概念：它初始化一个可学习的概念嵌入，使用它提示分割，并通过最小化概念分割误差的梯度更新该嵌入。我们进一步引入了混合不确定性估计（HUE）模块，该模块计算预测熵并将概念预测映射回框提示，测量概念-几何提示不一致性。高不确定性样本被主动标记以供人工纠正，纠正后的掩码则反馈给M2C以持续搜索更精确的概念嵌入，形成一个只需最少专家努力的自增强标注循环。在医学分割基准上的实验表明，我们的方法达到了最先进的少样本分割性能和卓越的标注效率，为可扩展的医学图像标注提供了一条实用高效的途径。代码位于https://github.com/Huster-Hq/M2C。

## Abstract
Transforming foundation segmentation models from human-prompted tools into auto-promptable annotators is critical for scalable medical data annotation. Current methods commonly depend on external feature matchers or auxiliary networks to automate geometric prompting, but introducing architectural overhead and limiting performance scalability. Although SAM3 natively supports concept segmentation via reusable text prompts, its direct use in medical imaging is hindered by a lack of fine-grained clinical knowledge and the ambiguity of human-written descriptions. In this work, we propose Mask to Concept (M2C), an efficient framework that adapts SAM3 for medical few-shot annotation without external modules, parameter retraining, or manual text engineering. Using only a few labeled images, M2C enables SAM3 to automatically search for transferable visual concepts entirely within its frozen architecture: it initializes a learnable concept embedding, uses it to prompt segmentation, and updates the embedding by gradients of minimizing the concept segmentation error. We further introduce a Hybrid Uncertainty Estimation (HUE) module that calculates the prediction entropy and maps concept predictions back to the box prompts, measuring concept-geometry prompting inconsistency. Highly uncertain samples are flagged actively for human correction, and the corrected masks are then fed back to M2C to continuously search for more precise concept embeddings, forming a self-enhancing annotation loop with minimal expert effort. Experiments on medical segmentation benchmarks show that our method achieves SOTA few-shot segmentation performance and outstanding annotation efficiency, offering a practical and efficient pathway toward scalable medical image labeling. Codes are at https://github.com/Huster-Hq/M2C.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：高质量医学图像标注成本高昂，现有方法要么依赖大量训练数据（训练型方法泛化差），要么引入外部特征匹配模块或辅助网络来自动生成提示（增加架构开销且未充分挖掘SAM内部推理能力）。SAM3原生支持通过文本提示进行概念分割，但医学图像缺乏细粒度临床知识，人工文本描述模糊且不可靠。
- **整体含义**：旨在将SAM3从人工提示工具转变为自动提示标注器，仅需少量标注样本，无需外部模块或参数重训练，实现高效、可扩展的医学图像少样本标注与主动学习闭环。

### 2. 方法核心思想、关键技术细节
- **核心思想**：在冻结的SAM3架构内，通过**可学习的概念嵌入**替代模糊的文本提示，利用梯度下降从少量标注掩码中搜索最优视觉概念嵌入，实现跨实例的自动化分割。
- **关键技术细节**：
  - **Mask to Concept (M2C)**：初始化一个可学习向量 \( E \)，将其与文本嵌入 \( \text{TEnc}(T) \) 相加后输入SAM3。在每次迭代中，利用已有标注样本，冻结SAM3全部参数，仅优化 \( E \)，通过最小化分割损失（IoU损失+交叉熵损失）更新嵌入。公式为：
    \[
    E_t = E_{t-1} - \eta \nabla_{E_{t-1}} \left( \frac{1}{|D_{\text{label}}^{t-1}|} \sum_{i} \mathcal{L}_{\text{seg}}(y_{\text{pred}}^{i}, y_{\text{label}}^{i}) \right)
    \]
    其中 \( y_{\text{pred}} = \text{SAM}(x, \text{TEnc}(T) + E) \)。文本提示T在搜索中逐渐不再必要。
  - **混合不确定性估计 (HUE)**：组合两个子度量：
    - 预测熵：对预测图中熵大于0.8的模糊像素取平均熵。
    - 概念-几何提示不一致性：将概念预测转为边界框，作为几何提示再生成掩码，计算二者IoU的补值（1 - IoU）。
    总不确定性 \( U_{\text{HU}} = U_{\text{ENT}} + U_{\text{CGPI}} \)。根据阈值 \( \tau \) 将样本分为“困难”（需要人工校正）和“容易”（直接接受预测），形成主动学习循环。

### 3. 实验设计
- **数据集**：
  - Kvasir-SEG（内窥镜息肉分割，1000张图片）
  - ISIC-2017（皮肤病变分割，仅使用官方测试集600张，避免数据泄露）
- **Benchmark**：少样本分割（FSS）性能与标注效率。设置1-shot、5-shot、10-shot三种设置，采用Dice系数评估。
- **对比方法**：
  - 基于训练的方法：Tyche、Multiverseg
  - 无需训练（基于SAM）的方法：ProtoSAM、Matcher、SPFS-SAM、MAUP
- **实验充分性**：包含定量对比（表1）、定性可视化（图2）、标注效率对比（图3）、消融实验（图4及表2：HUE vs Random、子指标消融）。

### 4. 资源与算力
- 文中明确提到：嵌入优化在**单张NVIDIA RTX 4090 GPU**上执行，使用AdamW优化器，学习率从5×10⁻⁴余弦衰减至5×10⁻⁶。**未提及总训练时长或迭代次数**，但指出搜索过程是快速的。

### 5. 实验数量与充分性
- **实验数量**：约7组主要实验（两个数据集×三种shot设置，加上标注效率曲线、消融实验）。
- **充分性评价**：较为充分。覆盖不同数据集（不同模态）、不同shot数、与多种SOTA方法对比、消融验证HUE各组件贡献。但缺少对更大规模数据集（如多器官）或更多元化场景的验证，且未报道时间成本（每轮搜索耗时等）。

### 6. 主要结论与发现
- M2C在少样本分割上显著优于所有对比方法：1-shot下，Kvasir-SEG Dice达76.3%（第二为72.1%），ISIC-2017 Dice达75.9%（第二为64.1%），提升幅度大。
- 标注效率曲线显示，M2C能以最少的人工标注样本数快速达到高整体Dice得分，且泛化性良好。
- HUE的有效性：结合预测熵和提示不一致性比单独使用任一子指标更能降低人工标注成本（从34.5%降至15.5%的样本需人工标注即可达到90% Dice）。

### 7. 优点
- **无需外部模块或重训练**：完全在冻结的SAM3架构内工作，轻量化，可快速适配新数据集。
- **无需手工文本工程**：通过梯度搜索自动找到最优视觉概念，避免模糊文本描述带来的歧义。
- **主动学习闭环**：HUE模块能可靠识别高不确定性样本，引导人工校正，持续改进概念嵌入，降低总体人力成本。
- **高效与可扩展性**：仅需少量标注样本即可启动，且性能随更多标注样本自然提升。

### 8. 不足与局限
- **算力与时间未详细报告**：未给出每轮搜索的具体耗时，难以评估实际部署的实时性。
- **实验覆盖有限**：仅在两个医学数据集（息肉、皮肤病变）上验证，缺少更丰富场景（如不同器官、3D体积数据、多类别分割）的评估。
- **依赖SAM3本身**：若SAM3在特定域的分割基础能力弱，M2C的搜索上限受限于模型。
- **阈值敏感**：HUE中熵阈值（0.8）、不确定性阈值（0.6）需手动设定，可能在不同数据集上需调整，未讨论自适应策略。
- **未处理多类别**：方法针对单概念（单类）设计，若需同时分割多个病变类型，需扩展为多概念搜索。

（完）
