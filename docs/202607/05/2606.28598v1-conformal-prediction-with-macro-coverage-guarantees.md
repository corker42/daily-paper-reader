---
title: Conformal Prediction with Macro-Coverage Guarantees
title_zh: 具有宏覆盖率保证的保形预测
authors: "Aabesh Bhattacharyya, Tiffany Ding, Rina Foygel Barber"
date: 2026-06-26
pdf: "https://arxiv.org/pdf/2606.28598v1"
tags: ["query:fundus-mc"]
score: 8.0
evidence: 共形预测提供宏覆盖保证，适用于长尾医学分类任务
tldr: 针对长尾分类中边缘覆盖率忽视稀有类、类条件覆盖率数据需求高的问题，本文提出利用标签加权共形预测实现有限样本宏观覆盖率保证。通过定义广义宏观覆盖率目标（类分组的加权平均覆盖率），推导出最小预测集的解析形式并设计对应评分函数。在ImageNet等数据集上验证方法能在保证整体可靠性的同时提升稀有类覆盖。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.28598v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1459, \"height\": 435, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28598v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1475, \"height\": 433, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28598v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1443, \"height\": 434, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28598v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1459, \"height\": 432, \"label\": \"Table\"}]"
motivation: 长尾分类中，类条件覆盖率对稀有类要求苛刻，而边缘覆盖率可能忽略低概率标签；宏观覆盖率作为折衷，但缺乏有限样本保证。
method: 采用标签加权共形预测，对每个类赋予权重，构造加权共形分数，理论证明所得预测集满足有限样本广义宏观覆盖率保证。
result: 在ImageNet和CIFAR-LT上，所提方法在给定宏观覆盖率下可生成更小的预测集，稀有类覆盖率显著高于边缘共形预测。
conclusion: 本文为共形预测提供了灵活的宏观覆盖率框架，在保持有限样本保证的同时适应长尾分布，可推广到任意类分组。
---

## 摘要
预测集应具有高覆盖率才有用，但某些覆盖率概念比其他概念更具实际相关性。在分类设置中，类条件覆盖率要求预测集（即新测试点的候选标签集）必须在每个类别内达到目标准确率水平，当许多类别稀少且校准点很少时，这可能难以满足。另一个极端是边际覆盖率，仅要求覆盖率在所有类别的分布上平均成立，这可能导致低概率标签基本被忽略。为了找到折中方案，近期工作引入了宏覆盖率，定义为类条件覆盖率的未加权平均值。宏覆盖率在边际覆盖率和类条件覆盖率之间提供了一种折中，特别适用于长尾设置。在这项工作中，我们表明标签加权保形预测可用于生成具有有限样本宏覆盖率保证的预测集，更一般地，还能保证一系列广义宏覆盖率目标，这些目标聚合了任意类别分组的覆盖率并取加权平均值。我们进一步刻画了满足给定广义宏覆盖率目标的最小预测集的形式，并提出了相应的保形评分函数。我们在两个大规模图像分类数据集上验证了我们的理论结果。

## Abstract
Prediction sets should have high coverage to be useful, but some coverage notions are more practically relevant than others. In the classification setting, class-conditional coverage requires that the prediction set (i.e., the set of candidate labels for a new test point) must achieve the target accuracy level within each class, which may be challenging to satisfy when many classes are rare and have few calibration points. At the other extreme, marginal coverage requires only that coverage holds on average over the distribution of all classes, which can lead to low-probability labels being essentially ignored. To find a middle ground, recent work has introduced macro-coverage, defined as the unweighted average of class-conditional coverages. Macro-coverage offers a compromise between marginal coverage and class-conditional coverage that is particularly appropriate for long-tailed settings. In this work, we show that label-weighted conformal prediction can be used to produce prediction sets with a finite-sample macro-coverage guarantee, and more generally a guarantee on a family of generalized macro-coverage objectives that aggregate coverage at the level of arbitrary class groupings and take a weighted average. We further characterize the form of the smallest prediction sets satisfying a given generalized macro-coverage objective and propose a corresponding conformal score function. We validate our theoretical results on two large-scale image classification datasets.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **长尾分类中的覆盖率困境**：在标准保形预测中，边缘覆盖率（marginal coverage）仅要求总体正确率，但低概率（稀有）类别可能被严重低估；类条件覆盖率（class-conditional coverage）虽能保护每个类别，但在稀有类数据极度稀疏时会导致预测集过大甚至无效。
- **宏覆盖率（macro-coverage）作为折中**：近期工作提出宏覆盖率，即所有类条件覆盖率的未加权平均，每个类别拥有相同权重，适合长尾分布。然而，现有方法缺乏**有限样本保证**，难以直接满足用户指定的宏覆盖水平。
- **本文目标**：提出一种**标签加权保形预测**方法，能够以有限样本保证实现（广义）宏覆盖率，并推导出满足该目标的最小预测集及其最优评分函数。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：利用标签加权保形预测，对每个类别（或分组）赋予权重，构造加权经验分布，取分位数作为阈值，从而直接控制宏覆盖率。
- **关键技术细节**：
  - **广义宏覆盖率**：设分组函数 \( g: \mathcal{Y} \to \mathcal{K} \)，权重函数 \( w: \mathcal{K} \to \mathbb{R}_{\ge 0} \)（可依赖校准数据的部分信息），则（广义）宏覆盖率为 \( \text{MacroCov}_{g,w}(C) = \sum_{k} w(k) \cdot P(Y \in C(X) \mid g(Y)=k) \)。
  - **算法1（标签加权保形预测）**：
    1. 对每个校准样本计算评分 \( s(X_i,Y_i) \) 和组别 \( G_i = g(Y_i) \)。
    2. 对每个组 \( k \)，统计样本数 \( N_k \)，若 \( N_k>0 \) 则构建经验分布 \( P_k = \frac{1}{N_k} \sum_{i\in I_k} \delta_{S_i} \)，否则 \( P_k = \delta_{\infty} \)。
    3. 计算修正的误覆盖率 \( \alpha' = \alpha - \max_{k: N_k>0} \frac{w(k)}{N_k} \)。
    4. 若 \( \alpha' \ge 0 \)，则预测集为 \( C(x) = \{ y : s(x,y) \le Q_{1-\alpha'} (\sum_k w(k) P_k) \} \)；否则 \( C(x) = \mathcal{Y} \)。
  - **理论保证（定理1 & 推论1）**：该预测集满足 \( \text{MacroCov}_{g,w}(C \mid H) \ge 1 - \alpha \)（条件于组别信息 \( H \)）。
  - **最优评分函数**：在给定（固定）权重下，使期望集大小最小的最优集形式为 \( C_t^*(x) = \{ y : \frac{w(g(y))}{\rho(g(y))} p(y|x) \ge t \} \)，对应评分函数 \( \hat{s}_{g,w}(x,y) = -\frac{w(g(y))}{\hat{\rho}(g(y))} \hat{p}(y|x) \)（使用估计量）。
  - **多约束同时保证**：若需同时满足多个宏覆盖率条件（如边缘+宏覆盖率），只需取各条件所得阈值的最大值（命题2）。

### 3. 实验设计：数据集、场景、benchmark、对比方法
- **数据集**：
  - **Pl@ntNet-300K**：植物物种图像，330类，高度长尾（最频繁类/最稀有类比例约100:1）。
  - **iNaturalist 2018**：动植物图像，857类，中度长尾（比例约10:1）。
  - 使用Ding et al. (2026)提供的截断版本（过滤掉测试样本过少的类别）。
- **场景**：长尾分类下的宏覆盖率目标。具体包括：
  - 普通宏覆盖率（均匀权重）。
  - 尾聚焦宏覆盖率（对10%最稀有类赋予λ=10倍权重）。
  - 属级宏覆盖率（按属分组，组内均匀权重）。
- **对比方法**：
  - **STANDARD**：标准保形预测（保证边缘覆盖率）。
  - **CLASSWISE**：类条件保形预测（保证每类覆盖率）。
  - **LABEL-WEIGHTED**：本文提出的标签加权保形预测。
- **评分函数**：基础softmax（\(-\hat{p}(y|x)\)）和本文提出的最优评分 \( \hat{s}_{g,w} \)。
- **评估指标**：宏覆盖率（MacroCov）、平均集大小（AvgSize）、边缘覆盖率（MarginalCov）。所有结果基于20次随机分割计算均值和标准误。

### 4. 资源与算力
- 论文**未明确说明**所使用的GPU型号、数量、训练时长等具体硬件信息。仅提及使用预训练ResNet-50进行分类器微调，并获取softmax分数。计算资源细节缺失。

### 5. 实验数量与充分性
- **实验数量**：主要实验结果包括两个数据集（Pl@ntNet, iNaturalist）、两个目标α水平（0.05, 0.1）下的宏覆盖率（表1-2）；两个广义宏覆盖率（尾聚焦、属级）实验（表3-4，同样两个α水平）；以及附录中的补充表格。
- **充分性**：实验覆盖了不同覆盖率目标、不同权重设计，并对比了三种代表性方法（标准、类条件、标签加权）。使用20次随机分割报告标准误，统计可靠。但缺乏对更大数据集（如ImageNet）或更多评分函数的对比，也未在非图像任务（如文本分类）上验证。

### 6. 论文的主要结论与发现
- 标准保形预测（边缘覆盖率）**不能**保证宏覆盖率，尤其在高度长尾的Pl@ntNet上差距明显。
- 类条件保形预测虽然能保证宏覆盖率，但预测集过大（平均大小高出几十倍）。
- 本文的**标签加权保形预测**能够精确达到目标宏覆盖率，且使用最优评分 \( \hat{s}_{g,w} \) 时平均集大小最小，显著优于softmax评分。
- 尾聚焦和属级宏覆盖率实验同样验证了方法的有效性，标签加权能在保持覆盖率的同时大幅缩小集大小。
- 同时满足多个覆盖率条件时，取最大阈值即可实现。

### 7. 优点：方法或实验设计上的亮点
- **理论完备**：提供了有限样本的宏覆盖率保证，且保证条件于组别信息，比无条件保证更强。
- **灵活性**：允许用户自定义分组和权重，权重甚至可依赖校准数据的部分信息（如组大小），便于实际中规避无信息组。
- **近似最优集**：推导了最优评分函数，并给出可实现的估计，使预测集在满足覆盖率的同时尽可能小。
- **多约束扩展**：支持同时满足多个宏覆盖率目标，且解法简单。
- **实验扎实**：采用真实长尾数据集、多个α、多个目标、多次随机分割，统计严谨。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
- **计算资源未报告**：缺乏训练和校准的硬件成本信息，影响可复现性评估。
- **实验覆盖有限**：仅使用两个图像数据集，未在自然语言处理或医学领域验证；评分函数仅比较了softmax和最优评分，未探讨其他常见评分（如APS）。
- **极端稀疏情况**：当某些组在校准中完全缺失时，需赋予无穷大阈值，导致预测集可能为全集；权重调整虽可缓解但未系统分析。
- **最优评分的依赖性**：\( \hat{s}_{g,w} \) 依赖于 \( \hat{p}(y|x) \) 和 \( \hat{\rho} \)，若分类器估计偏差大，可能影响效率（但覆盖率保证仍成立）。
- **所有权重依赖校准数据**：权重可以依赖于H（组计数），但论文未深入讨论如何自适应选择权重来平衡覆盖率与集大小。
- **仅限分类任务**：方法设计针对离散标签空间，难以直接推广到回归等连续输出场景。

（完）
