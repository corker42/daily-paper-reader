---
title: "Theoretical Foundations of $\\max$@$k$ Reinforcement Learning"
title_zh: "$\\max$@$k$ 强化学习的理论基础"
authors: "Riccardo Poiani, Martino Bernasconi, Andrea Celli"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.17823v1"
tags: ["query:fsrh"]
score: 8.0
evidence: "强化学习max@k理论"
tldr: "在代码生成、定理证明等困难任务中，通常用生成K次回答的max@k指标评估agent，但此类准则的学习理论不足。本文证明优化max@k目标与标准期望回报最大化本质不同，马尔可夫策略通常不足，并提出紧凑状态增强方法恢复最优性，同时刻画了历史依赖与非历史依赖策略的性能差距。此外，学习max@k最优策略统计上更难，本文给出达到最优样本复杂度的高效算法，为相关应用奠定理论基础。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17823v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 583, \"height\": 324, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17823v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 420, \"height\": 282, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17823v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 879, \"height\": 1058, \"label\": \"Figure\"}]"
motivation: "max@k指标广泛应用于困难任务的真实评估场景，但缺乏对其学习理论的基础性理解，无法保证最优策略的可学习性。"
method: "分析max@k目标与期望回报的本质差异，证明马尔可夫策略最优性缺失，提出一种状态增强方法恢复最优性，并设计高效学习算法。"
result: "学习max@k最优策略的统计复杂度高于标准RL，且所提算法实现了最优样本复杂度，同时量化了策略历史依赖带来的性能差距。"
conclusion: "为max@k强化学习提供了理论基石，揭示了其与标准RL的根本差异，指导了实际算法设计与评估指标选择。"
---

## 摘要
强化学习是现代大型推理模型的关键技术。通常，对于代码生成和定理证明等困难任务，智能体通过生成 $K$ 个响应（而非仅采样单个响应）来评估，并采用诸如 $\max$@$k$ 的具有重试意识的指标来衡量性能。尽管这些准则在实践中至关重要，但其学习的理论基础仍然有限。本文对有限时域强化学习中的 $\max$@$k$ 学习问题进行了理论研究。我们证明，优化 $\max$@$k$ 目标与标准期望回报最大化存在根本性差异。特别地，我们证明了马尔可夫策略通常不够充分，识别出一种能恢复最优性的紧凑状态增广方法，并显式刻画了依赖历史策略与非依赖历史策略之间可能出现的性能差距。此外，我们表明学习 $\max$@$k$ 最优策略在统计上比标准强化学习更具难度，并给出了一种达到最优样本复杂度的高效算法。

## Abstract
Reinforcement Learning is a cornerstone technique for modern large reasoning models. Usually, for difficult tasks such as code generation and theorem proving, the agent is evaluated by generating $K$ responses rather than sampling a single response, and performance is then measured using a retry-aware metric such as $\max$@$k$. Despite their practical importance, the theoretical foundations of learning under such criteria remain limited. In this work, we provide a theoretical study of the $\max$@$k$ learning problem in finite-horizon reinforcement learning. We show that optimizing the $\max$@$k$ objectives is fundamentally different from standard expected-return maximization. In particular, we prove that Markovian policies are in general insufficient, identify a compact state augmentation that restores optimality, and explicitly characterize the performance gap that can arise between history-dependent and non-history-dependent policies. Moreover, we show that learning $\max$@$k$-optimal policies is statistically harder than standard reinforcement learning and provide an efficient algorithm that achieves the optimal sample complexity rate.

---

## 论文详细总结（自动生成）

### 论文的核心问题与整体含义

- **研究动机与背景**：在代码生成、定理证明等复杂任务中，大型语言模型（LLM）常通过生成多个候选答案并取最优（即 `max@k` 指标）来提升性能。然而，现有的强化学习理论主要基于期望累积回报的线性性，而 `max@k` 目标是非线性的，因此缺乏针对该目标的学习理论。本文旨在填补这一空白，为 `max@k` 强化学习提供严格的理论基础，解释为何标准 RL 的方法（如期望回报最大化）在该设置下可能不够最优，并设计高效的学习算法。

### 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：`max@k` 目标改变了策略优化的本质，最优策略需要依赖历史信息（如之前 rollouts 的最佳回报和当前 rollouts 的累计回报），而非仅依赖当前状态。本文通过状态增广和离散化技术实现了高效的近似规划和学习。

- **关键技术细节**：
  - **马尔可夫策略的不足**：构造反例（如安全 vs 冒险动作的 MDP），证明存在 `max@k` 目标下马尔可夫策略的性能与最优历史依赖策略之间存在大于常数的乘法差距（如 1.24 倍）。
  - **紧凑历史压缩**：证明只需保留两个关键信息即可实现最优：之前 rollouts 的最大回报 \(M\) 和当前 rollouts 的累计回报 \(G\)，形成压缩状态 \((s, M, G)\)，从而将历史依赖策略空间缩小至 \(\Pi_C\)。
  - **近似规划算法**：对奖励函数进行均匀离散化（网格步长 \(\kappa\)），在离散化后的 MDP 上通过逆向归纳求解最优策略。理论保证：可得到 \(2\kappa H\) 近似的策略，时间复杂度为 \(O(KS^2 AH^3/\kappa^2)\)。令 \(\kappa = \epsilon/H\) 即得 \(O(H^5 S^2 A/\epsilon^2)\) 的 FPTAS。
  - **精确规划的难解性**：通过从子集和问题（Subset-Sum）的归约，证明对于 \(K=2\) 且策略空间大小恒定的情形，精确值判定问题在指数精度下是 NP-hard 的，因此近似方案是紧的。
  - **学习下界**：构造时间齐次 MDP 实例，证明任何 \((\epsilon, \delta)\)-正确算法需要的样本复杂度下界为 \(\Omega(K H^3 S A \epsilon^{-2} \log(1/\delta))\)，比标准 RL 多出因子 \(K\)。核心思想：小概率高回报事件需要更精确估计。
  - **学习上界算法**：采用均匀采样（每个状态-动作对-时间步采样 \(m\) 次）构建经验转移核，对奖励离散化，然后在经验模型上求解最优策略。通过精心的误差分析（方差引理、模拟引理），证明当 \(m = \tilde{O}(S K^2 H^2 \log(1/\delta) + K H^3 \epsilon^{-2} \log(1/\delta))\) 时，算法达到 \(\epsilon\) 最优，样本复杂度在主导项上匹配下界（\(\tilde{O}(K S A H^3 / \epsilon^2)\)）。

- **公式/算法流程**（文字说明）：
  1. 输入：MDP、K、H、精度 ϵ、失败概率 δ。
  2. 对每个 (s, a, h) 采样 m 次得到经验转移核 \(\hat{p}_h(\cdot|s,a)\)。
  3. 定义离散化奖励 \(\tilde{r}_{h,\kappa}(s,a)\)，其中 \(\kappa = \epsilon/(9H)\)。
  4. 在扩展状态空间 \(\mathcal{X} = \mathcal{S} \times \lceil H/\kappa \rceil \times \lceil H/\kappa \rceil\) 上，用经验转移核和离散奖励构建有限时域 MDP。
  5. 通过逆向归纳求解该 MDP 的最优（确定性）策略 \(\hat{\mu}\)（属于压缩策略类 \(\Pi_C\)）。
  6. 输出 \(\hat{\mu}\)。

### 实验设计

- **数据集/场景**：本文是纯理论工作，没有在真实数据集或基准上进行数值实验。理论分析基于构造的 MDP 实例（如单状态、两动作、小概率成功等）来证明下界和差距。
- **Benchmark**：比较对象是标准 RL 的期望回报目标（本文称为经典 RL），并与现有理论结果（如 Gheshlaghi Azar et al. 2013 的样本复杂度下界）进行对比。
- **对比方法**：未涉及具体算法对比，而是从理论上将 `max@k` 学习与标准 RL 学习进行对比，证明其样本复杂度多出因子 K。

### 资源与算力

- 文中未提及任何 GPU 型号、数量或训练时长。作为理论论文，不涉及大规模计算实验。

### 实验数量与充分性

- 本文为纯理论贡献，无传统实验。“实验”环节体现为：
  - **构造反例（图1）**：证明马尔可夫策略次优性。
  - **构造反例（图3）**：证明遗漏 G 或 M 均会失优。
  - **下界实例（图2）**：证明样本复杂度线性依赖 K。
  - **归约证明（子集和问题）**：证明精确规划 NP-hard。
- 这些构造性证明在理论上是充分的、严格的，覆盖了规划和学习的主要方面（策略类的最优性、计算复杂性、样本复杂度上下界）。没有进行数值验证，但数学证明提供了坚实的保证。

### 论文的主要结论与发现

1. `max@k` 目标下，马尔可夫策略一般不是最优的；历史依赖策略能带来至少 1.24 倍（常数因子）的性能提升。
2. 最优策略可以压缩为仅依赖当前状态、之前最佳回报 \(M\) 和当前累计回报 \(G\)，从而将策略空间从全历史降低到多项式大小。
3. 存在一个 FPTAS（多项式时间近似方案）用于求解 `max@k` 规划问题，但精确规划是 NP-hard 的（归约自子集和问题）。
4. 学习 `max@k` 最优策略在统计上比标准 RL 更难：样本复杂度下界为 \(\Omega(K H^3 S A \epsilon^{-2} \log(1/\delta))\)，比经典 RL 多出因子 K。
5. 提出的均匀采样 + 离散化算法达到匹配上界 \(\tilde{O}(K S A H^3 / \epsilon^2)\)，因此该下界是紧的。
6. 从概念上看，这些理论结果与实践中自适应推理方法（如 self-refinement、Tree-of-Thoughts）的设计原则一致：保留历史有用信息但仅需压缩表示。

### 优点

- **填补理论空白**：首次系统研究 `max@k` 强化学习的理论基础，揭示了与标准 RL 的根本差异。
- **方法巧妙**：通过奖励离散化和状态压缩，将非马尔可夫问题转化为有限时域 MDP 上的标准问题，并获得 FPTAS。
- **证明全面**：覆盖了规划复杂性、策略最优性、统计下界和上界，结果相互呼应（上下界匹配）。
- **连接实践**：不仅给出理论保证，还指出自适应推理方法在概念上更优，为实际算法设计提供指导。

### 不足与局限

- **理论假设较强**：假设生成模型（generative model），即可以任意查询任何状态-动作-时间步的下一状态分布。实际交互学习（forward interaction）尚未分析，这是未来方向。
- **奖励函数有界**：假设奖励在 [0,1] 内，但可推广至任意有界奖励。
- **未考虑函数逼近**：论文局限于表格 MDP，未讨论大规模状态空间下的函数近似情况。
- **无数值实验**：尽管理论严格，但缺乏数值验证以展示算法在实际问题（如 LLM 微调）中的效果。
- **应用限制**：理论结果主要针对有限时域离散 MDP，能否直接应用于连续状态/动作或部分可观测环境尚不明确。

（完）
