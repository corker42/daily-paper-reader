Title: The Sample Complexity of Policy Learning with Mu-Resets

URL Source: https://arxiv.org/pdf/2608.07772v1

Published Time: Tue, 11 Aug 2026 00:16:10 GMT

Number of Pages: 9

Markdown Content:
# The Sample Complexity of Policy Learning with μ-Resets 

Gene Li 

gene@ttic.edu 

August 11, 2026 

Abstract 

We study policy-based reinforcement learning under the μ-resets interaction protocol of Kakade and Langford [KL02]. This interaction protocol enables the learner to sample trajectories from a given exploratory reset distribution μ, in addition to the starting distribution. We resolve the question raised by [KLS25] on the role of policy realizability for the sample complexity of this problem. Critically, the dependence on horizon H is governed by the notion of coverage assumed of the reset distribution. Under bounded all-policy concentrability, we show a exp(Ω( H)) sample complexity lower bound; with bounded pushforward concentrability, we show the dependence on horizon is tightly characterized as exp(Θ( √H)) .

# 1 Introduction 

Kakade and Langford’s influential paper [KL02] introduced the μ-resets interaction protocol for reinforcement learning (RL). 

Definition 1 (μ-Resets) . The learner is given online sampling access to an exploratory reset distribution μ over states. The learner can either roll out trajectories from the initial state distribution or from the reset distribution μ.

In this paper, we study the sample complexity of policy learning under the μ-resets interaction protocol: given a policy class Π, how many trajectories does one need to collect in order to find a near-optimal policy bπ

satisfying V bπ ≥ max π∈Π V π − ε?Two classical algorithms have been proposed for this setting: Conservative Policy Iteration [KL02] for discounted, infinite-horizon RL and Policy Search by Dynamic Programming (PSDP) [BKSN03] for finite-horizon RL. Both require a particularly stringent assumption on the representational capacity of Π called 

policy completeness . Under policy completeness, their sample complexities are polynomial in the relevant problem parameters. Informally, policy completeness requires that the policy class is closed under the policy improvement operator. A priori, it is unclear if this assumption is fundamentally necessary, or just needed to get the analysis to work. 

Quick aside on notation. In the remainder of the paper, we focus on episodic, finite-horizon RL over MDPs 

M = ( X , A, H, P, R, d 1) with (large, but finite) layered state space X = FHh=1 Xh, action set A with cardinality 

|A| = A, horizon H, transition function P , bounded rewards R, and initial state distribution d1. Hence, the exploratory reset distribution μ = {μh}Hh=1 consists of distributions μh ∈ ∆( Xh). We assume μ1 = d1 (only making the learner’s job harder); in every round of interaction, the learner picks a reset layer h ∈ [H] and roll out a trajectory from μh. We assume the cumulative reward P 

> h

rh in every trajectory is bounded in [0 , 1] .[KLS25] study the sample complexity of policy learning with μ-resets without the assumption of policy completeness. In the agnostic policy learning setting, they show an information-theoretic sample complexity lower bound of exp(Ω( H)) . However, they left open the setting where the policy class satisfies realizability 

(that the optimal policy π⋆ ∈ Π). Policy realizability is implied by policy completeness but is a much weaker (and arguably more natural) assumption. Unfortunately, the aforementioned exp(Ω( H)) lower bound crucially requires the non-realizability of Π. [KLS25] additionally show that with bounded pushforward concentrability (to be defined below), PSDP achieves exp(Θ( H)) sample complexity under policy realizability via a new upper bound analysis and algorithm-dependent lower bound for PSDP. In an information-theoretic sense, the sample complexity of RL under μ-resets with policy realizability has remained completely open. 1arXiv:2608.07772v1 [cs.LG] 7 Aug 2026 Our results. We characterize the sample complexity of RL under μ-resets when the policy class is realizable. We first list three well-studied coverage conditions which can be used to characterize the quality of the reset distribution μ.1 In what follows, we use dπh (·) ∈ ∆( Xh) to denote the policy occupancy measure. 

Definition 2 (Policy-class concentrability) . The policy-class concentrability coefficient is 

Cconc (μ; Π , M ) := max  

> π∈Π

max 

> h∈[H]

dπh

μh ∞

.

Definition 3 (All-policy concentrability) . Let Πall denote the set of all Markov policies on M . The all-policy concentrability coefficient is 

Call (μ; M ) := max 

> π∈Πall

max 

> h∈[H]

dπh

μh ∞

.

Definition 4 (Pushforward concentrability) . The pushforward concentrability coefficient is 

Cpush (μ; M ) := max  

> h∈[H−1]

max 

> x∈X h, a ∈A

P (· | x, a )

μh+1 ∞

.

It is straightforward from the definitions to show that Cconc (μ; Π , M ) ≤ Call (μ; M ) ≤ Cpush (μ; M ). When clear from the context, we write Cconc , Call , and Cpush .Our main results are stated below and summarized in Table 1. 

Theorem 1 (Lower bound for Call ). For any sufficiently large H ∈ N, there exists a policy class Π of size 2H ; a family of MDPs M over a state space of size 2O(H), binary action space, and horizon H; and known reset distribution 

μ; such that every M ∈ M satisfies (i) Π is realizable, and (ii) μ satisfies Call (μ; M ) ≤ 4. Any proper deterministic algorithm that returns a 1/8-optimal policy with constant probability must sample at least 2Ω( H) trajectories for some MDP in M.

Thus, if we assume the reset has bounded Call , we still require the stringent policy completeness assumption in order to get sample-efficient learning, and realizability doesn’t buy us anything. Next, we turn to the assumption of bounded pushforward concentrability of the reset μ.

Theorem 2 (Upper bound for Cpush ). Let M be an MDP and suppose Π is a realizable class of policies. Furthermore, suppose the reset distributions μ satisfy pushforward concentrability with parameter Cpush . Then for every ε, δ ∈ (0 , 1) ,there is an algorithm using μ-resets that, with probability at least 1 − δ, returns an ε-optimal policy bπ using at most 

O (AC push )2√H H3/2

ε2 log H|Π|

δ

!

trajectories .

Theorem 3 (Lower bound for Cpush ). For any sufficiently large H ∈ N, there exists a policy class Π of size 2√H ; a family of MDPs M over a state space of size 2O(√H), binary action space, and horizon H; and a known reset distribution 

μ; such that every M ∈ M satisfies (i) Π is realizable, and (ii) μ satisfies Cpush (μ; M ) ≤ 8. Any proper deterministic algorithm that returns a 1/8-optimal policy with constant probability must sample at least 2Ω( √H) trajectories for some MDP in M.

Theorem 3 can be viewed as an information-theoretic strengthening of the algorithmic lower bound of [Theorem 8, KLS25] that additionally builds in √H-length combination locks into the construction. Although these bounds remain exponential, together Theorem 2 and 3 show that bounded pushforward concentrability changes the horizon dependence from exp(Θ( H)) to exp(Θ( √H)) . This comparison has three consequences. First, realizability yields an exponential improvement over the agnostic setting, where [Theorem 3 of KLS25] prove a 2Ω( H) lower bound. Second, PSDP requires exp(Θ( H)) samples under the same realizability and pushforward assumptions [Theorems 6 and 8 of KLS25], and is therefore suboptimal. Third, our results give a quantitative separation between all-policy and pushforward concentrability, analogous to separations known in offline RL [FKSX22; JRSW24; XJ21].   

> 1Throughout we use the convention that 1/0 = ∞.

2Cconc Call Cpush 

Policy Completeness 

✓

[BKSN03], see also [Thm. 1 of KLS25] 

=⇒ ✓ =⇒ ✓

Policy Realizability ✗ ⇐= ✗

Thm. 1 

✗⋆

Thms. 2–3 Agnostic ✗ ⇐= ✗ ⇐= ✗

[Thm. 3 of KLS25] Table 1: Sample complexity under μ-resets. Rows denote the different representational assumptions on 

Π; columns denote coverage assumptions on the reset μ. A ✓ denotes poly( C, A, H log |Π|, ε −1) sample complexity (where C ∈ { Ccov , C all , C push }), while ✗ denotes a exp(Ω( H)) lower bound. The starred entry is instead exp(Θ( √H)) for constant A and Cpush . Arrows show implications of upper and lower bounds to weaker statements. 

Statement of AI Use. Results were obtained via GPT 5.6 Pro, with assistance from Charlie Hou and Xuchen You. We used GPT 5.6 Sol Codex to aid in writing and presentation. 

# 2 Lower Bound under All-Policy Concentrability 

In this section, we sketch the proof of Theorem 1. We first describe the construction and then explain why reset access does not help. We omit the formal information-theoretic arguments, which follow the standard analysis for rich-observation combination locks [SDM+21; JLR+23; KLS25]. 

Lower bound construction. We use a variant of the rich-observation combination lock, which is essentially a Block MDP with enormous decoder class. See Figure 1 for an illustration. For simplicity, assume that H is even. We let A = {0, 1} and define the policy class to be open-loop policies: 

Π = π : πh(x) ≡ ah for every x ∈ X h, (a1, . . . , a H ) ∈ { 0, 1}H , thus, |Π| = 2 H .

Each MDP instance is parameterized by an optimal policy π⋆ ∈ Π. Its first H/ 2 actions determine the value from the initial state; its actions from layer H/ 2 + 1 through layer H − 1 serve only to mask information in reset trajectories. The final action is irrelevant. Every layer has four latent states: a good state sG 

> h

, a bad state sB 

> h

, a verifier state sV 

> h

, and a neutral state 

sN 

> h

. The initial latent state is sG 

> 1

. In the first half of the MDP, the good–bad and verifier–neutral pairs form two combination locks. The optimal action keeps the learner on the good or verifier track, whereas an incorrect action transits the learner from sG 

> h

to sB 

> h+1

and from sV 

> h

to sN

> h+1

. In the second half, the good/neutral states advance under either action, while the verifier and bad states form a new “poison” combination lock. Formally, for every h ∈ [H − 1] , the latent dynamics Plat are 

Plat (· | sG 

> h

, a ) = 



δsG 

> h+1

a = π⋆h and h ≤ H/ 2,δsG 

> h+1

h > H/ 2,δsB 

> h+1

otherwise ,Plat (· | sB 

> h

, a ) = δsB 

> h+1

,Plat (· | sV 

> h

, a ) = 



δsV 

> h+1

a = π⋆h,δsN 

> h+1

a̸ = π⋆h and h ≤ H/ 2,δsB 

> h+1

a̸ = π⋆h and h > H/ 2,Plat (· | sN 

> h

, a ) = δsN 

> h+1

.

3Blue arrow: a = π⋆h Red arrow: a̸ = π⋆h Black arrow: either action sh ∈ supp( μh)              

> layer 1 layer 2 layer H/ 2layer H/ 2 + 1 layer H/ 2 + 2 layer H−1layer Hterminal reward Good Bad Verifier Neutral

sG 

> 1

sG

> 2

sB

> 2

sV

> 2

sN

> 2

sG

> H/ 2

sB

> H/ 2

sV

> H/ 2

sN

> H/ 2

sG

> H/ 2+1

sB

> H/ 2+1

sV

> H/ 2+1

sN

> H/ 2+1

sG

> H/ 2+2

sB

> H/ 2+2

sV

> H/ 2+2

sN

> H/ 2+2

sG

> H−1

sB

> H−1

sV

> H−1

sN

> H−1

sG

> H

sB

> H

sV

> H

sN

> H

R = 12

R = 0 

R = 1 

R = 12

· · · · · · Figure 1: Construction used for proof of Theorem 1. Rewards are nonzero only in the final layer: 

Rlat (s, a ) = 



1 s = sV 

> H

, 

> 12

s ∈ { sG 

> H

, s N 

> H

},

0 otherwise .

With the latent dynamics fully specified, we can define the set of Block MDP instances which are parameterized by optimal policy π⋆ ∈ Π and decoder ϕ ∈ Φ, as M = {Mπ⋆,ϕ : π⋆ ∈ Π, ϕ ∈ Φ}. To define the decoder class, we use the standard idea from [SDM+21; KLS25]: for every layer let Xh be a large (observed) state space of size m = 2 cH for some sufficiently large c > 0 and define Φ to be the set of all possible decoders 

ϕ : X → S which assign equal number of (observed) states to every latent state. A transition to s ∈ S h emits an observation according to the emission function ψ(s) = Unif  {x ∈ X h : ϕ(x) = s}.Finally, the reset distribution μ is defined as: 

μh,ϕ =



ψ(sG 

> 1

), h = 1 , 

> 14

ψ(sG 

> h

) + 14 ψ(sB 

> h

) + 14 ψ(sV 

> h

) + 14 ψ(sN 

> h

), 2 ≤ h ≤ H/ 2, 

> 12

ψ(sG 

> h

) + 12 ψ(sB 

> h

), H/ 2 < h ≤ H. 

That is, the first state is emitted from the good state; for the other layers in the first half, the reset is uniform over all four latent states; the second-half resets are uniform over the good and bad states. It is clear that for every π⋆ ∈ Π and ϕ ∈ Φ,

Call (μϕ; Mπ⋆,ϕ ) ≤ 4.

Lastly, one can also check that π⋆ is optimal under these dynamics from any state, and it achieves value of 

1/2 from the starting distribution μ1.

Why doesn’t reset access allow sample-efficient learning of the optimal policy? Fix an instance Mπ⋆,ϕ .The large decoder class Φ has statistical complexity log |Φ| which is exponential in H. With high probability, every observed state is a fresh, nonrepeated observation. Thus, transition data from Mπ⋆,ϕ leaks essentially no information about the underlying π⋆, and the learner is forced to learn π⋆ from reward observations. 4Observe that a trajectory from the initial distribution only achieves positive reward iff the first H/ 2 actions exactly match π⋆, thus requiring 2Ω( H) samples to learn. Therefore, the learner must also try to utilize the trajectories rolled out from resets. We next argue that this is also doomed to fail: • Rolling out from resets h > H/ 2 are useless - no matter what policy the learner plays, they will see rewards of 1/2 and 0 equally often. • Rolling out from resets 2 ≤ h ≤ H/ 2 is a more delicate case. Fix any partial policy πh:H−1 ∈ { 0, 1}H−h,and let Z be the random variable representing the reward we observe by sampling a state Unif( Xh)

then rolling out with πh:H−1. Then if πh:H̸ = π⋆h:H , then Z is 1/2 or 0 with equal probability. Also, if 

πh:H = π⋆h:H , then Z is 1 with probability 1/4, 1/2 with probability 1/2, and 0 with probability 1/4.This calculation elucidates the role of the “poison” combination lock: it effectively prevents the learner from just doing backwards induction from layer H/ 2 because this will only work if the learner already knows poison suffix π⋆H/ 2: H−1 (which itself can only be randomly guessed with exponentially small probability). Thus, together with the initial distribution argument, we have argued that there is no way for the learner to use 2O(H) samples to properly identify π⋆ given a random instance from M.

# 3 Upper Bound under Pushforward Concentrability 

In this section, we prove our main upper bound using BlockPSDP , a blockwise variant of PSDP whose pseudocode is given in Algorithm 1. 

Algorithm. Fix a number of blocks K ∈ [H] and let L = H/K . We may assume that L is an integer; otherwise, we can pad the last block. For each k ∈ [K], set 

start k = 1 + ( k − 1) L, Block k = {start k, . . . , kL }.

The algorithm works backward over these blocks. At block k, it explores uniformly until the end of the block and then follows the suffix already learned on later blocks. Here ◦ denotes layerwise concatenation of partial policies. We write Πh for the restriction of Π to layer h, and ΠBlock k for its restriction to Block k.Recall that PSDP learns the policy via resets one layer at a time. Under pushforward concentrability, its error can amplify by a factor of Cpush at each layer [KLS25]. The key idea of BlockPSDP is to learn L consecutive layers at once by importance sampling trajectories that explore uniformly within a block. This costs AL in sample complexity, but error amplification occurs only across the K − 1 boundaries between the K = H/L 

blocks. Balancing these two terms gives the desired sample complexity. 

Proof of Theorem 2. The analysis has two steps: estimation within each block and error propagation across block boundaries. 

Importance sampling guarantee. Fix a block k, and suppose we have constructed bπstart k+1 :H . For πBlock k ∈

ΠBlock k , define 

Jk(πBlock k ) = Ex∼μstart k

h

V πBlock k ◦bπstart k+1 :H 

> start k

(x)

i

.

The empirical objective in Algorithm 1 is the standard trajectory importance sampling estimate for Jk(πBlock k ).Standard concentration for importance sampling [JLR+23] implies that for any η ∈ (0 , 1] , as long as 

n ≥ c AL

η2 log 2|Π|

δ , (1) for some sufficiently large c > 0, then, with probability at least 1 − δ,

Jk

 bπBlock k

 ≥ Jk

 π⋆

> Block k

 − η. (2) 5Algorithm 1 BlockPSDP 

Input: Reset distributions μ = {μh}h∈[H], policy class Π, number of blocks K, and sample size n.

1: for k = K, . . . , 1 do 

2: Initialize dataset Dk = ∅.

3: for n times do 

4: Sample xstart k ∼ μstart k and aBlock k ∼ Unif( AL), generating τk = ( xh, a h)h∈Block k .

5: Let vk := PHh=start k rh be the return from executing aBlock k ◦ bπstart k+1 :H from xstart k .

6: Set Dk ← D k ∪ { (τk, v k)}.

7: Call policy optimization oracle: bπBlock k ∈ argmax  

> πBlock k∈ΠBlock k

AL

n

X  

> (τk,v k)∈D k

1{ah = πh(xh) ∀h ∈ Block k} vk.

8: Set bπstart k :H ← bπBlock k ◦ bπstart k+1 :H .

9: return bπ1: H .

Lemma 1 (Error propagation) . For k ∈ [K], let 

ek = Ex∼μstart k

h

V π⋆ 

> start k

(x) − V bπstart k :H 

> start k

(x)

i

.

On the event (2) ,

eK ≤ η, ek ≤ η + Cpush ek+1 for k < K. (3) 

Proof. For k < K , let νk+1 denote the distribution of xstart k+1 obtained by drawing xstart k ∼ μstart k and executing π⋆ 

> Block k

. Then 

ek = Jk

 π⋆

> Block k

 − Jk

 bπBlock k

 + Ex∼νk+1 

h

V π⋆ 

> start k+1

(x) − V bπstart k+1 :H 

> start k+1

(x)

i| {z }

> continuation gap

≤ η + Ex∼νk+1 

h

V π⋆ 

> start k+1

(x) − V bπstart k+1 :H 

> start k+1

(x)

i

≤ η + Cpush ek+1 .

The last line uses pushforward concentrability as well as the optimality of π⋆, which ensures that the continuation gap between policies is pointwise nonnegative. For the last block there is no continuation gap, so we have 

eK = JK

 π⋆

> Block K

 − JK

 bπBlock K

 ≤ η. 

Final guarantee. At every block let the number of rollouts be 

n = O ALC2( K−1)  

> push

K2

ε2 log 2|Π|

δ

!

.

Then by the importance sampling guarantee, Lemma 1, and a union bound, with probability at least 1 − Kδ ,

V π⋆

− V bπ1: H = e1 ≤ ε. For simplicity, take K = L = √H and assume these quantities are integers. Replacing 

δ by δ/K gives success probability at least 1 − δ and 

nK = O (AC push )2√H H3/2

ε2 log H|Π|

δ

!

.

This completes the proof of Theorem 2. 64 Lower Bound under Pushforward Concentrability 

In this section, we sketch the proof of Theorem 3. We first describe the construction and then explain why reset access does not help. As in Section 2, we omit the formal information-theoretic argument, which follows from a standard random-decoder analysis and the reward identity below. We use K blocks of length L, so that H = KL . For the lower bound, take K = L = √H and assume these quantities are integers. 

Lower bound construction. We use a recursive rich-observation combination lock; see Figure 2 for an illustration. Again let A = {0, 1}. Partition the horizon into Block 1, . . . , Block K , where 

Block k := {(k − 1) L + 1 , . . . , kL }, k ∈ [K].

An MDP instance is parameterized by a hidden key θ = ( θ1, . . . , θ L) ∈ { 0, 1}L. The policy class consists of open-loop policies which repeat the same length-L action sequence in every block. Namely, for any 

ϑ ∈ { 0, 1}L, define the policy πϑ as 

πϑh (x) = ϑℓ when h = ( k − 1) L + ℓ, 

and let Π = {πϑ : ϑ ∈ { 0, 1}L}. Thus, |Π| = 2 L = 2 √H . We design the MDP instance so that π⋆ = πθ .Every layer has four latent states: a real state sR

> h

, a decoy state sD 

> h

, a one state s1

> h

, and a zero state s0

> h

. The initial latent state is sR 

> 1

. For p ∈ [0 , 1] , define 

Collect h+1 (p) := pδ s1 

> h+1

+ (1 − p)δs0 

> h+1

, pk := 4 −k, k ∈ [K].

We now describe the dynamics. Fix h = ( k − 1) L + ℓ ∈ [H − 1] .• The one and zero states progress to their respective next states under either action: 

Plat (· | s1

> h

, a ) = δs1 

> h+1

, Plat (· | s0

> h

, a ) = δs0 

> h+1

.

• In every block, the real–zero pair forms a combination lock. From a real state, the correct action θℓ

advances to the next real state, while a wrong action moves to the zero state. At the end of a nonfinal block k, the correct action transitions to Collect (pk). Thus, 

Plat (· | sR

> h

, a ) = 



δsR 

> h+1

a = θℓ, ℓ < L, 

Collect h+1 (pk) k < K, a = θL, ℓ = L, δs0 

> h+1

otherwise .

• The decoy states prevent the learner from discovering the key by working in the opposite direction. In a nonfinal block k, a wrong action transitions to Collect (pk+1 ). Correct actions advance along the decoy track, and completing the block moves to a real state at the start of block k + 1 . In the final block, either action moves to the zero state. Formally, 

Plat (· | sD 

> h

, a ) = 



Collect h+1 (pk+1 ) k < K, a ̸ = θℓ,δsD 

> h+1

k < K, a = θℓ, ℓ < L, δsR 

> h+1

k < K, a = θL, ℓ = L, δs0 

> h+1

k = K. 

All rewards before layer H are zero. At layer H, the two nonzero reward distributions are 

RH (s, a ) = 

(

1 s = s1 

> H

,

Ber( pK ) s = sR 

> H

, a = θL.

Every other terminal state-action pair receives reward zero. 7We realize these dynamics as a rich-observation Block MDP using the same decoder and emission construction as in Section 2. Let m = 2 c√H for a sufficiently large constant c > 0. For each h ≥ 2, let |X h| = 8 m, and let the decoder ϕ partition Xh into the preimages of sR

> h

, s D 

> h

, s 0

> h

, s 1 

> h

with respective sizes m, 4m, 2m, m . A transition to a latent state emits uniformly from its preimage. This defines the family Mθ,ϕ , indexed by the hidden key θ

and the decoder ϕ.We take μ1 to be the initial distribution and μh = Unif( Xh) for every h ≥ 2. If X ∼ μh and S = ϕ(X), then 

P(S = sR

> h

) = 18 , P(S = sD 

> h

) = 12 , P(S = s0

> h

) = 14 , P(S = s1

> h

) = 18 .

From this, it is immediate that every instance satisfies 

Cpush (μ; Mθ,ϕ ) ≤ 8.

Also, πθ is optimal from every state. At a real state, it plays the unique rewarding suffix. At a decoy state in a nonfinal block k, it completes the current suffix and the next block, obtaining value pk+1 . A deviation while the process remains on the decoy track immediately transitions to Collect (pk+1 ), while completing the current block leads to a real state whose value is at most pk+1 . In the final block, every policy has value zero from a decoy state, and actions do not affect the value from the one and zero states. Consequently, 

V πθ

= p1 = 14 , V πϑ

= 0 for every ϑ̸ = θ. 

Why doesn’t reset access allow sample-efficient learning of the optimal policy? Fix an instance Mθ,ϕ . As in Section 2, the large random decoder ensures that transition observations leak essentially no information about θ, so the learner is forced to use reward observations. Similar to the lower bound in Section 2, online trajectories from the initial state distribution μ1 require 2Ω( L)

samples to guess the key θ. We next explain why reset trajectories do not allow the learner to recover the key one bit at a time. Fix a reset layer h = ( k − 1) L + ℓ ≥ 2 in a nonfinal block k < K and an action sequence through the end of block k + 1 . Let 

I = 1{ah:kL = θℓ:L}, (action matches the remaining suffix of Block k),J = 1akL +1:( k+1) L = θ , (action matches θ in Block k+1 ).

The rewards are either zero or one, so we compute the probability of the reward being 1: 

P(rH = 1) = 18|{z}  

> from s1
> h

+ pk

8 I

|{z}  

> from sR
> h

+ pk+1 

2 (1 − I + IJ )

| {z } 

> from sD
> h

= 18 + pk

8 (1 + IJ ), since pk+1 = 14 pk.

Therefore, every action sequence except the one matching the remaining suffix of Block k and all of Block k+1 

produces the same reward law. This is the essential recursive poisoning property: pushforward coverage places reset mass on the next-block real state, and the decoy at that block poisons the new signal again at a scale four times smaller. What about in the final block? A reset reaches the real state with probability 1/8, so the gap between the correct and incorrect final actions is pK /8. Thus, standard hypothesis testing bounds require Ω( p−2 

> K

) = 2 Ω( K)

samples to identify θL.Combining the two sources of information gives a 2Ω(min {K,L }) trajectory lower bound. Taking K = L = √H

yields the claimed 2Ω( √H) lower bound. 8Blue arrow: a = θℓ Red arrow: a̸ = θℓ Black arrow: either action sh ∈ supp( μh)                                 

> Block 1Block 2Block K
> layer 1 layer 2 layer Llayer L+ 1 layer L+ 2 layer 2Llayer H−L+ 1 layer H−1layer Hterminal reward Real Decoy One Zero
> sR
> 1sR
> 2
> sD
> 2
> s12
> s02
> sR
> L
> sD
> L
> s1
> L
> s0
> L
> sR
> L+1
> sD
> L+1
> s1
> L+1
> s0
> L+1
> sR
> L+2
> sD
> L+2
> s1
> L+2
> s0
> L+2
> sR
> 2L
> sD
> 2L
> s12L
> s02L
> sR
> H−L+1
> sD
> H−L+1
> s1
> H−L+1
> s0
> H−L+1
> sR
> H−1
> sD
> H−1
> s1
> H−1
> s0
> H−1
> sR
> H
> sD
> H
> s1
> H
> s0
> H
> R∼Ber( pK)
> R= 0
> R= 0
> R= 1
> R= 0

· · ·   

> Collect (p1)
> Collect (p2)Collect (p3)

· · · · · · · · · Figure 2: Construction used for proof of Theorem 3. 

# References 

[BKSN03] J. A. Bagnell, S. M. Kakade, J. G. Schneider, and A. Y. Ng. Policy search by dynamic programming. In Advances in Neural Information Processing Systems 16 , pages 831–838, 2003. [FKSX22] D. J. Foster, A. Krishnamurthy, D. Simchi-Levi, and Y. Xu. Offline reinforcement learning: Funda-mental barriers for value function approximation. In Proceedings of the 35th Conference on Learning Theory ,volume 178 of Proceedings of Machine Learning Research , page 3489, 2022. Full version: arXiv:2111.10919. [JLR+23] Z. Jia, G. Li, A. Rakhlin, A. Sekhari, and N. Srebro. When is agnostic reinforcement learning statistically tractable? In Advances in Neural Information Processing Systems 36 , 2023. [JRSW24] Z. Jia, A. Rakhlin, A. Sekhari, and C.-Y. Wei. Offline reinforcement learning: Role of state ag-gregation and trajectory data. In Proceedings of the 37th Conference on Learning Theory , volume 247 of 

Proceedings of Machine Learning Research , pages 2644–2719, 2024. [KL02] S. M. Kakade and J. Langford. Approximately optimal approximate reinforcement learning. In 

Proceedings of the 19th International Conference on Machine Learning , pages 267–274, 2002. [KLS25] A. Krishnamurthy, G. Li, and A. Sekhari. The role of environment access in agnostic reinforcement learning. In Proceedings of the 38th Conference on Learning Theory , volume 291 of Proceedings of Machine Learning Research , pages 3405–3406, 2025. Full version: arXiv:2504.05405. [SDM+21] A. Sekhari, C. Dann, M. Mohri, Y. Mansour, and K. Sridharan. Agnostic reinforcement learning with low-rank MDPs and rich observations. In Advances in Neural Information Processing Systems 34 ,2021. [XJ21] T. Xie and N. Jiang. Batch value-function approximation with only realizability. In Proceedings of the 38th International Conference on Machine Learning , volume 139 of Proceedings of Machine Learning Research ,pages 11404–11413, 2021. 9