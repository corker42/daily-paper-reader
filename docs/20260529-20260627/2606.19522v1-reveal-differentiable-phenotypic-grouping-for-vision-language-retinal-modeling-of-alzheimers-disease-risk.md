---
title: "REVEAL++: Differentiable Phenotypic Grouping for Vision-Language Retinal Modeling of Alzheimer's Disease Risk"
title_zh: "REVEAL++: 用于阿尔茨海默病风险视觉-语言视网膜建模的可微表型分组"
authors: "Ethan Elio Meidinger, Seowung Leem, Zeyun Zhao, Ruogu Fang"
date: 2026-06-17
pdf: "https://arxiv.org/pdf/2606.19522v1"
tags: ["query:fundus-mc"]
score: 8.0
evidence: 眼底图像分析深度学习；用于AD风险的视觉-语言视网膜建模
tldr: 针对视网膜图像预测阿尔茨海默病风险中现有离散分组对比学习脱离表征学习的问题，提出REVEAL++，将表型相似性建模为可微连续权重，通过软多正例对比学习联合优化跨模态对齐与表型结构。在UK Biobank数据上优于离散分组方法和标准视觉语言基线，为多模态神经退行性疾病风险建模提供了更稳健的框架。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.19522v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1238, \"height\": 658}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.19522v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 901, \"height\": 265}, {\"url\": \"assets/tables/arxiv/2606.19522v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1270, \"height\": 543}]"
motivation: 现有对比学习采用离散表型分组，固定硬分组脱离表征学习，无法反映疾病风险的连续谱本质。
method: 用可微连续权重函数建模个体间相似性，定义软多正例关系，提出软目标对比损失实现端到端联合学习。
result: 在UK Biobank视网膜图像上，预测阿尔茨海默病风险优于离散分组对比学习和标准视觉语言基线。
conclusion: 将表型相似性作为可学习连续信号，为多模态视网膜数据建模神经退行性风险提供新范式。
---

## 摘要
视网膜为神经退行性疾病提供了一个无创的窗口，能够捕捉与未来认知衰退风险相关的细微结构模式。诸如REVEAL等视觉-语言对齐框架表明，将视网膜眼底图像与结构化的临床风险叙述配对，能够改善阿尔茨海默病（AD）的早期预测。这些方法的一个关键设计选择是使用表型分组，即在对比学习中将具有相似风险特征的个体视为多元正例对。然而，现有方法将表型相似性视为离散概念，依赖于硬分组分配，这施加了严格的监督并将分组形成与表示学习分离。我们在对比学习中提出了表型结构的连续形式。我们不将样本分配到固定聚类，而是将受试者间相似性建模为一个可微加权函数，该函数源自视网膜图像和风险特征中模态内嵌入的相似性。这些权重通过连续聚合算子定义了软多元正例关系，实现了反映疾病风险谱的梯度监督。我们进一步引入了一个软目标对比目标函数，以端到端的方式联合学习跨模态对齐和表型结构。在用于事件性AD预测的UK Biobank视网膜成像数据上评估，所提出的框架始终优于基于离散分组的对比学习和标准视觉-语言基线。通过将表型相似性视为可学习的连续信号而非固定分组规则，我们的方法为基于多模态视网膜和临床数据的人群规模神经退行性风险建模提供了原则性和鲁棒的基础。

## Abstract
The retina offers a noninvasive window into neurodegenerative disease, capturing subtle structural patterns associated with a risk of future cognitive decline. Vision-language alignment frameworks such as REVEAL have shown that pairing retinal fundus images with structured clinical risk narratives improves early prediction of Alzheimer's disease (AD). A key design choice in these approaches is the use of phenotypic grouping, where individuals with similar risk profiles are treated as multi-positive pairs during contrastive learning. However, existing methods operationalize phenotypic similarity as a discrete construct, relying on hard group assignments that impose rigid supervision and decouple group formation from representation learning. We propose a continuous formulation of phenotypic structure within contrastive learning. Rather than assigning samples to fixed clusters, we model inter-subject similarity as a differentiable weighting function derived from intra-modality embedding similarities in both retinal images and risk profiles. These weights define soft multi-positive relationships through a continuous aggregation operator, enabling graded supervision that reflects the spectrum nature of disease risk. We further introduce a soft-target contrastive objective that jointly learns cross-modal alignment and phenotypic structure in an end-to-end manner. Evaluated on UK Biobank retinal imaging data for incident AD prediction, the proposed framework consistently outperforms discrete group-based contrastive learning and standard vision-language baselines. By treating phenotypic similarity as a learnable, continuous signal rather than a fixed grouping rule, our approach provides a principled and robust foundation for population-scale neurodegenerative risk modeling from multi-modal retinal and clinical data.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

阿尔茨海默病（AD）存在漫长的临床前阶段，病理变化（如淀粉样蛋白沉积、tau蛋白积累、血管功能障碍等）持续积累多年后才出现认知症状。视网膜作为中枢神经系统的“窗口”，其微血管和结构变化可反映神经退行性风险，且具有无创、可大规模筛查的优势。现有工作（如REVEAL）通过视觉-语言对比学习将视网膜图像与结构化临床风险叙事对齐，并引入**离散表型分组**（hard grouping）将相似风险个体视为多正例对。然而，**AD风险本质上是连续的、异质性的谱系**，硬分组施加了人为边界，且分组过程与表示学习相分离，无法适应数据中的渐进结构。为此，本文提出 REVEAL++，将表型相似性建模为**可微连续权重**，使对比学习的监督强度随表型接近程度平滑变化，从而更真实地反映疾病风险的连续本质。

### 2. 论文提出的方法论

#### 核心思想
用连续加权函数代替离散聚类，将个体间的表型相似性作为一种可微的软监督信号，融入对比学习。

#### 关键技术细节

1. **编码器与嵌入**：图像编码器为 RETFound，文本编码器为 GatorTron（冻结）。两者通过线性投影映射到共享的 d=1024 维空间，并进行 ℓ2 归一化得到 \(\hat{z}_p^I\) 和 \(\hat{z}_p^T\)。
2. **模态内相似度**：分别计算图像嵌入相似度矩阵 \(S_{ii}(p,q)\) 和文本嵌入相似度矩阵 \(S_{tt}(p,q)\)（余弦相似度）。
3. **可微表型加权**：对每个模态的相似度，通过带可学习阈值 \(\tau_F,\tau_T\) 和锐度参数 \(g_F,g_T\) 的 sigmoid 门控得到 \(a_F(p,q), a_T(p,q)\)。然后使用概率并算符（\(W_{pq}=1-(1-a_F)(1-a_T)\)）融合两个模态的信号，得到最终权重矩阵 \(W\in[0,1]^{N\times N}\)。
4. **软多正例对比损失**：定义跨模态相似度 \(S_{it}(p,q)=\langle\hat{z}_p^I,\hat{z}_q^T\rangle\)，经温度缩放和偏置后得到 logit \(\ell_{pq}=S_{it}(p,q)/\tau-\beta\)。损失函数为：
   \[
   \mathcal{L}_{MP}=\frac1{N^2}\sum_{p,q}\Big[W_{pq}\log(1+e^{-\ell_{pq}})+(1-W_{pq})\log(1+e^{\ell_{pq}})\Big]
   \]
   当 \(W_{pq}\) 接近1时，该对被视为正例；接近0时视为负例；中间值提供软监督。

整个框架是端到端可微的，阈值和锐度参数均由网络学习，对比损失驱动跨模态对齐和表型结构协同优化。

### 3. 实验设计

- **数据集**：UK Biobank，使用基线评估的结构化问卷数据（人口学、行为、认知、生活方式等风险因素）和彩色眼底照片（CFP）。经过质量控制和翻转右眼图像后，共约 39,242 名受试者（训练 30,462，验证 3,384，测试 5,396）。
- **基准方法**：
  - 基础模型：SVM（结构化变量 + CFP 特征）、RETFound-CFP（仅图像）、BiomedCLIP、RETCLIP、PMC-CLIP。
  - 多模态基线：RETFound+GatorTron（拼接嵌入 + SVM）、REVEAL（无GACL）、REVEAL（有GACL，即离散分组）。
- **评估指标**：AUROC、Balanced Accuracy、F1、MCC。所有实验重复 10 次随机种子，报告平均值±标准差。
- **训练细节**：batch size=128，优化器 AdamW（lr=2.42e-4, ϵ=8.61e-7, weight decay=0.0232），超参数由 Optuna 搜索。图像编码器微调，文本编码器冻结。阈值从开发集 85% 数据的余弦相似度分布（上四分位）初始化。

### 4. 资源与算力

论文**未明确说明**使用的 GPU 型号、数量或训练时长。仅在实现细节中提及 batch size 和优化参数，未提供计算资源的具体信息。

### 5. 实验数量与充分性

- **实验数量**：主实验为 AD 事件预测（表2），对比了 8 种方法（含自身两个消融变体），重复10次随机种子。
- **充分性**：对比方法包括纯图像、纯文本、通用生物医学视觉-语言模型（BiomedCLIP、PMC-CLIP）、领域特定模型（RETFound、RETCLIP）以及 REVEAL 的两个变体（无GACL、有GACL），覆盖充分。但**缺乏对可微权重中阈值、锐度参数的消融研究**（例如固定阈值 vs 学习阈值、模态融合方式的影响）。实验仅在一个数据集（UK Biobank）上进行，外部泛化性待验证。
- **客观性与公平性**：所有方法使用相同多模态SVM评估协议，报告均值和标准差，统计可信。但未提供置信区间或显著性检验。

### 6. 论文的主要结论与发现

REVEAL++ 在 AUROC、Balanced Accuracy、F1、MCC 四个指标上均显著优于所有对比方法（例如 AUROC 0.678 ，比次优的 REVEAL+GACL 的 0.658 高出约 2 个点）。这表明**可微连续表型加权比硬分组能更有效地建模疾病风险的谱系结构**，获得更优的跨模态表示和下游预测性能。该框架体现了“性质相似度作为可学习连续信号”的生物学合理性。

### 7. 优点

- **创新性强**：首次将表型相似度建模为可微连续权重，与对比学习端到端联合优化，而不是预先聚类或阈值硬分组，更符合神经退行性疾病的连续病理过程。
- **技术优雅**：使用 sigmoid 门控+可学习参数和概率并算符，实现软多正例监督，公式简洁且理论上易于扩展。
- **效果显著**：在 UK Biobank 上达到 SOTA，证明了方法在人口级数据中的有效性。
- **框架可迁移**：可推广至其他慢性病（如心血管疾病）的多模态风险建模。

### 8. 不足与局限

- **实验覆盖不足**：仅在 UK Biobank 一个数据集上验证，缺乏在多种族、多中心数据上的泛化评估。AD 发病预测样本量可能不均衡（事件数少），但论文未提供数据分布细节。
- **缺乏关键消融**：未分析不同模态相似度组合策略（如只使用图像相似度、只使用文本、不同融合算子）、未分析阈值初始化敏感度或锐度参数的影响。
- **计算资源未说明**：无法评估训练成本与可复现性。
- **推理效率未讨论**：虽然训练端到端，但推理时仍需要同时计算文本和图像嵌入，可能增加延迟；未与轻量级方案比较。
- **临床意义有限**：仅报告了 AUC 等统计指标，未提供风险分的校准曲线或对不同风险层级的区分能力，未进行因果分析或可解释性评估。

（完）
